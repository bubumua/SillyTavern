# SillyTavern 项目与插件上手指南

## 0. 这份指南解决什么问题

这份指南面向已经把本仓库放在本机的使用者，默认路径是：

```powershell
D:\codesaves\SillyTavern
```

它重点讲 4 件事：

1. 怎么启动、理解和调试 SillyTavern。
2. 怎么理解角色卡、预设、世界书、聊天变量进入 prompt 的过程。
3. 怎么使用两个关键插件：
   - `JS-Slash-Runner`，也叫 Tavern Helper / 酒馆助手，用来运行外部 JavaScript 脚本。
   - `ST-Prompt-Template`，也叫 Prompt Template / 提示词模板，用来在 prompt、世界书、消息中执行 EJS/JavaScript 模板。
4. 怎么结合 `preset_and_chara/chara` 下的角色卡，尤其是 `性斗学园超级重制版current.json`，做可以上手演示的变量、EJS、世界书控制器和外部脚本示例。

适用条件：

- 本指南基于当前仓库快照，不保证适配未来上游 SillyTavern 的所有改动。
- 本指南把 `JS-Slash-Runner` 和 `ST-Prompt-Template` 当作“两个插件”来讲。
- 角色卡示例以 `preset_and_chara/chara/性斗学园/性斗学园超级重制版current.json` 为主，另参考 `MoST-2-7-7.json`、`命定之诗与黄昏之歌v4.2.json` 和 `欲望世界0407_extract_v3.json` 中的脚本导入方式。

---

## 1. 项目快速启动

### 1.1 安装依赖

在 PowerShell 7 中进入项目目录：

```powershell
cd D:\codesaves\SillyTavern
npm install
```

本项目是 Node.js ESM 项目，`package.json` 使用 `"type": "module"`。当前项目文档要求 Node.js `>=18`，你的本机 Node.js 24 可以运行。

### 1.2 启动服务器

常规启动：

```powershell
npm start
```

等价入口：

```powershell
node server.js
```

常用开发启动：

```powershell
npm run debug
npm run start:no-csrf
```

默认访问地址：

```text
http://localhost:8181
```

### 1.3 启动后看什么

启动后浏览器打开的是一个单页应用：

- 主 HTML：`public/index.html`
- 主前端入口：`public/script.js`
- 后端入口：`server.js -> src/server-main.js`
- 路由注册：`src/server-startup.js`
- Chat Completion 代理：`src/endpoints/backends/chat-completions.js`
- Prompt 转换：`src/prompt-converters.js`

验证服务器是否活着：

```powershell
Invoke-WebRequest http://localhost:8181 -UseBasicParsing | Select-Object StatusCode
```

预期看到：

```text
StatusCode
----------
       200
```

如果端口被占用，检查占用进程：

```powershell
Get-NetTCPConnection -LocalPort 8181 -ErrorAction SilentlyContinue |
    Select-Object LocalAddress,LocalPort,State,OwningProcess
```

---

## 2. SillyTavern 的核心模型：上下文编排系统

SillyTavern 不只是“把角色设定拼到用户消息前面”。更准确地说，它是一个上下文编排系统。

一次生成大致经过这条链路：

```text
public/script.js
  -> public/scripts/world-info.js
  -> public/scripts/openai.js
  -> src/prompt-converters.js
  -> src/endpoints/backends/chat-completions.js
```

每一层的职责：

| 层 | 主要文件 | 作用 |
|---|---|---|
| 前端生成入口 | `public/script.js` | 收集聊天历史、角色字段、persona、扩展注入、世界书输入 |
| 世界书引擎 | `public/scripts/world-info.js` | 根据关键字、常驻、递归、预算等规则决定哪些条目进入上下文 |
| Chat Completion 组装 | `public/scripts/openai.js` | 把 prompt 槽位和聊天历史整理成 messages |
| 协议转换 | `src/prompt-converters.js` | 转成 Claude、Gemini、Cohere、text completion 等供应商格式 |
| 后端代理 | `src/endpoints/backends/chat-completions.js` | 根据后端类型转发请求 |

你写角色卡和世界书时，真正影响结果的是这些问题：

- 内容来自哪里：角色卡、世界书、预设、作者注释、persona、扩展。
- 什么时候激活：常驻、关键词、EJS 条件、变量状态。
- 插到哪里：`before_char`、`after_char`、depth、Prompt Manager 槽位、`@INJECT`。
- 用什么角色发送：`system`、`user`、`assistant`。
- token 不够时谁被裁掉。

---

## 3. 角色卡、预设、世界书分别是什么

### 3.1 角色卡

角色卡一般是 PNG，内部通过 PNG metadata 存储 JSON。当前仓库还保留了很多已抽出的 JSON，便于直接阅读。

示例路径：

```text
preset_and_chara/chara/性斗学园/性斗学园超级重制版current.json
preset_and_chara/chara/MoST/MoST-2-7-7.json
preset_and_chara/chara/命定之诗与黄昏之歌/命定之诗与黄昏之歌v4.2.json
preset_and_chara/chara/欲望世界/欲望世界0407_extract_v3.json
```

角色卡常见字段：

```json
{
  "name": "角色名",
  "description": "角色描述",
  "personality": "性格",
  "scenario": "场景",
  "first_mes": "开场白",
  "mes_example": "示例对话",
  "data": {
    "creator_notes": "作者备注",
    "system_prompt": "角色卡 system prompt",
    "post_history_instructions": "后历史指令",
    "character_book": {
      "entries": []
    },
    "extensions": {}
  }
}
```

在 `性斗学园超级重制版current.json` 中，角色主体字段非常薄，真正复杂的逻辑主要放在：

```text
data.character_book.entries
data.extensions.regex_scripts
data.extensions.tavern_helper.scripts
```

这类卡不是靠单段角色描述驱动，而是靠“世界书 + 变量 + 外部脚本 + UI 正则替换”共同驱动。

### 3.2 预设

预设在：

```text
preset_and_chara/preset
```

示例：

```text
preset_and_chara/preset/Izumi 0406.json
preset_and_chara/preset/潮汐Chaoxi.json
preset_and_chara/preset/Kemini Dramatron 1.52.json
```

预设控制的是 prompt 槽位、顺序、启停和部分模型参数。它不是只保存一个 system prompt。

常见槽位包括：

```text
main
nsfw
dialogueExamples
jailbreak
chatHistory
worldInfoBefore
worldInfoAfter
charDescription
charPersonality
scenario
personaDescription
```

### 3.3 世界书

世界书条目不是“只要命中关键词就插入文本”这么简单。一个条目通常有：

```json
{
  "comment": "条目标题 / 备忘",
  "content": "实际注入内容",
  "constant": true,
  "selective": false,
  "key": ["关键词"],
  "keysecondary": [],
  "position": "before_char",
  "order": 100
}
```

`性斗学园超级重制版current.json` 中有 218 个内嵌世界书条目，其中很多是控制器，例如：

```text
EJS大型事件控制器
EJS角色控制器
EJS魅力阶段控制器
EJS堕落度阶段控制器
EJS地图控制器
EJS比赛控制器
[mvu_update]变量输出格式
[mvu_plot]正文格式（额外模型解析）
变量列表
```

这些条目说明它的机制是：

```text
变量状态 -> EJS 判断 -> 拉取或输出对应规则 -> 模型回复 -> MVU 更新变量 -> 下一轮继续
```

---

## 4. 插件一：JS-Slash-Runner / Tavern Helper / 酒馆助手

### 4.1 它解决什么问题

SillyTavern 原生不允许角色卡直接执行任意 JavaScript。`JS-Slash-Runner` 提供了一个受隔离的 iframe 脚本运行环境，让角色卡、预设或全局配置可以运行脚本。

本仓库中插件位置：

```text
data/default-user/extensions/JS-Slash-Runner
default-user/extensions/JS-Slash-Runner
```

它能做的事：

- 运行角色脚本、聊天脚本、预设脚本。
- 调用 Slash 命令。
- 监听生成、消息渲染、聊天切换等事件。
- 读写聊天变量。
- 注入或更新前端 UI。
- 加载远程 JavaScript 模块。
- 暴露全局接口供其他脚本使用。

安全边界：

- 外部脚本可能读取聊天记录、变量、部分页面状态。
- 外部脚本可能发起网络请求。
- 只导入可信来源。
- 对 `raw.githubusercontent.com`、`cdn.jsdelivr.net`、`testingcf.jsdelivr.net` 这类 URL，要确认仓库和分支是你信任的。

### 4.2 在角色卡中运行外部 JS

`性斗学园超级重制版current.json` 的 `data.extensions.tavern_helper.scripts` 中有这些导入：

```javascript
import 'https://testingcf.jsdelivr.net/gh/StageDog/tavern_resource/dist/酒馆助手/世界书强制自定义排序/index.js'
```

```javascript
import 'https://testingcf.jsdelivr.net/gh/MagicalAstrogy/MagVarUpdate@beta/artifact/bundle.js'
```

```javascript
import 'https://testingcf.jsdelivr.net/gh/StageDog/tavern_resource/dist/酒馆助手/自动更新角色卡/index.js'
```

```javascript
import 'https://cdn.jsdelivr.net/gh/vincentrong2005/Fatria/dist/性斗学园/性斗学园脚本/index.js'
```

还有一个本地开发版本：

```javascript
import
'http://localhost:5500/dist/性斗学园/性斗学园脚本/index.js'
```

含义：

- `import 'https://...'`：从 CDN 拉取远程脚本。
- `import 'http://localhost:5500/...'`：从本机开发服务器拉取脚本，适合你边改边刷新。
- 脚本通常不是世界书内容，而是前端逻辑、UI、变量框架、自动更新工具。

### 4.3 实操演示：新增一个角色脚本按钮

在酒馆助手的角色脚本里新增一个脚本，内容写：

```javascript
const { triggerSlash } = TavernHelper;

TavernHelper.registerButton?.({
    name: '显示变量',
    onClick: async () => {
        await triggerSlash('/echo severity=success 已点击角色脚本按钮');
    },
});
```

如果当前版本没有 `registerButton` 这个封装，就用酒馆助手 UI 自带的“脚本按钮”功能：给脚本配置一个按钮，然后在按钮触发时执行：

```javascript
await triggerSlash('/echo severity=success 按钮运行成功');
```

验证方式：

1. 打开角色卡。
2. 确认 `JS-Slash-Runner` 启用。
3. 点击脚本按钮。
4. 页面应出现成功提示。

### 4.4 实操演示：运行 Slash 命令

`JS-Slash-Runner` 的类型定义里有：

```typescript
declare function triggerSlash(command: string): Promise<string>;
```

最小例子：

```javascript
await triggerSlash('/echo severity=success Hello from Tavern Helper');
```

读 SillyTavern 宏：

```javascript
const lastMessageId = await triggerSlash('/pass {{lastMessageId}}');
await triggerSlash(`/echo severity=info 最后一楼 ID: ${lastMessageId}`);
```

注意：

- Slash 命令写错时可能没有明显反馈。
- 脚本里的字符串要注意引号和换行。
- 如果命令需要用户当前角色、当前聊天上下文，必须在角色和聊天都加载完成后执行。

### 4.5 实操演示：本地外部 JS 开发服务器

`性斗学园` 卡里有本地脚本地址：

```text
http://localhost:5500/dist/性斗学园/性斗学园脚本/index.js
```

这要求你的本机 `5500` 端口能提供一个目录，并且目录下有：

```text
dist/性斗学园/性斗学园脚本/index.js
```

如果你有 Fatria 脚本仓库，假设路径是：

```text
D:\codesaves\Fatria
```

可以这样启动静态服务器：

```powershell
cd D:\codesaves\Fatria
npx http-server . -p 5500 --cors
```

然后在浏览器验证：

```text
http://localhost:5500/dist/性斗学园/性斗学园脚本/index.js
```

如果能看到 JS 内容，酒馆助手就可以导入。

如果你只想做最小演示，也可以新建任意静态目录，放一个 `dist/demo/index.js`，然后脚本中写：

```javascript
console.info('[demo] 外部脚本已加载');
await triggerSlash('/echo severity=success 外部 JS 加载成功');
```

导入方式：

```javascript
import 'http://localhost:5500/dist/demo/index.js'
```

浏览器控制台应看到：

```text
[demo] 外部脚本已加载
```

页面应弹出：

```text
外部 JS 加载成功
```

### 4.6 共享全局接口

酒馆助手支持脚本之间共享接口。类型定义里有：

```typescript
function initializeGlobal(global: string, value: any): void;
function waitGlobalInitialized(global: string): Promise<any>;
```

脚本 A：

```javascript
initializeGlobal('MyCardTools', {
    version: '1.0.0',
    hello(name) {
        return `你好，${name}`;
    },
});
```

脚本 B：

```javascript
const tools = await waitGlobalInitialized('MyCardTools');
console.info(tools.hello('玩家'));
```

MVU 这类脚本通常也会通过类似方式暴露全局接口，所以其他脚本可以等待 `Mvu` 初始化后再调用。

---

## 5. 插件二：ST-Prompt-Template / 提示词模板

### 5.1 它解决什么问题

`ST-Prompt-Template` 让你可以在以下位置写 EJS / JavaScript 模板：

- 角色描述。
- 世界书条目。
- 预设 prompt。
- 消息内容。
- 渲染后的楼层 HTML。

本仓库中插件位置：

```text
data/default-user/extensions/ST-Prompt-Template
default-user/extensions/ST-Prompt-Template
```

它的核心能力：

- `<% ... %>` 执行 JavaScript。
- `<%- ... %>` 输出原始结果。
- `<%= ... %>` 输出转义结果。
- `getvar(...)` / `setvar(...)` 读写变量。
- `getwi(...)` 直接读取世界书条目内容。
- `activewi(...)` 让世界书条目进入原生激活流程。
- `[GENERATE:BEFORE]` / `[RENDER:AFTER]` 等内容注入。
- `@INJECT` 把内容作为单独 message 插入 prompt。

### 5.2 EJS 基本语法

执行代码，不直接输出：

```ejs
<% const favor = getvar('好感度', { defaults: 0 }); %>
```

输出变量：

```ejs
当前好感度：<%- getvar('好感度', { defaults: 0 }) %>
```

条件判断：

```ejs
<% if (getvar('好感度', { defaults: 0 }) >= 80) { %>
她已经非常信任 {{user}}，可以触发专属剧情。
<% } else { %>
她仍然保持距离，互动应更克制。
<% } %>
```

循环：

```ejs
<% const present = getvar('stat_data.关系系统.在场人物', { defaults: [] }); %>
当前在场人物：
<% for (const name of present) { %>
- <%- name %>
<% } %>
```

打印多段内容：

```ejs
<%
print('第一段规则');
print('第二段规则');
%>
```

注释：

```ejs
<%# 这段不会输出，也不会发给模型 %>
```

### 5.3 变量 API

读变量：

```javascript
getvar('stat_data.核心状态._魅力', { defaults: 0 });
```

写变量：

```javascript
setvar('好感度', 50);
```

指定作用域：

```javascript
setvar('调试.计数', 1, { scope: 'local' });
getvar('调试.计数', { scope: 'local', defaults: 0 });
```

自增：

```javascript
incvar('调试.计数', 1, { defaults: 0 });
```

自减：

```javascript
decvar('体力', 5, { defaults: 100, min: 0 });
```

常见作用域：

| scope | 用途 |
|---|---|
| `message` | 当前消息 / 楼层变量，适合角色卡状态快照 |
| `local` | 当前聊天变量 |
| `global` | 全局变量 |
| `cache` | 读取合并后的缓存视图 |
| `initial` | 初始变量 |

实际写卡时，最常见的是：

```javascript
getvar('stat_data.xxx')
setvar('stat_data.xxx', value)
```

`性斗学园` 的控制器大量读取：

```javascript
getvar('stat_data.时间系统.日期')
getvar('stat_data.时间系统.星期')
getvar('stat_data.位置系统.地点名称')
getvar('stat_data.核心状态._魅力')
getvar('stat_data.核心状态._幸运')
getvar('stat_data.关系系统.在场人物')
```

### 5.4 直接读取世界书：`getwi`

`getwi(...)` 是提示词模板插件里非常关键的函数。

它的含义不是“模拟关键词触发”，而是直接读取某个世界书条目的内容。

最小例子：

```ejs
<%- await getwi('高魅力阶段规则') %>
```

带条件：

```ejs
<%
const charm = Number(getvar('stat_data.核心状态._魅力', { defaults: 0 }));
%>
<% if (charm >= 80) { %>
<%- await getwi('魅力80以上专属规则') %>
<% } %>
```

指定世界书：

```ejs
<%- await getwi('世界书名称', '条目标题') %>
```

适用场景：

- 根据变量主动拉取规则。
- 根据位置拉取地图条目。
- 根据在场人物拉取角色条目。
- 根据日期拉取事件条目。

### 5.5 原生式激活：`activewi`

`activewi(...)` 的思路不同。它不是直接输出内容，而是把条目交给世界书激活流程。

```ejs
<% await activewi('夏洛特好感事件') %>
```

使用建议：

- 想绕过原生关键词、直接拿内容：用 `getwi`。
- 想保持原生世界书规则、排序、过滤：用 `activewi`。

### 5.6 内容注入标签

世界书条目标题可以带注入标签：

```text
[GENERATE:BEFORE]
[GENERATE:AFTER]
[RENDER:BEFORE]
[RENDER:AFTER]
[GENERATE:1:BEFORE]
[GENERATE:REGEX:你好]
[InitialVariables]
```

含义：

| 标签 | 作用 |
|---|---|
| `[GENERATE:BEFORE]` | 发送给 LLM 前，注入到 prompt 开头 |
| `[GENERATE:AFTER]` | 发送给 LLM 前，注入到 prompt 末尾 |
| `[RENDER:BEFORE]` | LLM 输出渲染时，插入显示内容开头，不发送给 LLM |
| `[RENDER:AFTER]` | LLM 输出渲染时，插入显示内容末尾，不发送给 LLM |
| `[GENERATE:REGEX:pattern]` | 当消息匹配正则时注入 |
| `[InitialVariables]` | 把条目内容作为初始变量树 |

示例：

```text
世界书条目标题：
[GENERATE:REGEX:战斗|挑战|决斗]

世界书条目内容：
检测到战斗相关输入。下一轮必须检查体力、战力、位置与对手等级。
```

### 5.7 Prompt 注入：`@INJECT`

`@INJECT` 用来把世界书条目内容插入为独立 message。

绝对位置：

```text
@INJECT pos=0,role=system
```

相对用户消息：

```text
@INJECT target=user,index=-1,at=before,role=system
```

正则匹配：

```text
@INJECT regex="战斗|决斗",at=before,role=system
```

示例条目：

```text
标题：
@INJECT pos=0,role=system

内容：
你必须优先遵循本条系统规则：所有状态结算必须基于变量，不得让用户直接声明结果成立。
```

注意：

- 对 Gemini、Claude 这类结构化 API，`system` 消息位置很重要。
- `@INJECT` 很强，但也更容易破坏 prompt 顺序。
- 指令类内容适合单独 system 消息；知识类内容不一定适合拆得太碎。

---

## 6. 变量系统：三套路径不要混用

这是最容易出错的地方。

同一个变量，在不同系统里写法不同。

假设变量实际含义是：

```text
stat_data.时间系统.日期
```

### 6.1 Prompt Template 读写变量

EJS / Prompt Template 里使用点路径：

```javascript
getvar('stat_data.时间系统.日期')
setvar('stat_data.时间系统.日期', '2025年03月18日')
```

### 6.2 Tavern Helper 宏读取变量

角色卡里常见：

```text
{{format_message_variable::stat_data}}
```

`性斗学园` 的“变量列表”条目就是：

```text
<status_current_variable>
{{format_message_variable::stat_data}}
</status_current_variable>
```

作用是把当前楼层变量中的 `stat_data` 格式化后放进 prompt，让模型知道当前状态。

### 6.3 MVU JSONPatch 更新变量

MVU 的 `<JSONPatch>` 路径通常相对 `stat_data` 根。

正确：

```xml
<UpdateVariable>
<JSONPatch>
[
  { "op": "replace", "path": "/时间系统/日期", "value": "2025年03月18日" },
  { "op": "delta", "path": "/核心状态/_魅力", "value": 1 }
]
</JSONPatch>
</UpdateVariable>
```

不要写成：

```json
{ "op": "replace", "path": "/stat_data/时间系统/日期", "value": "2025年03月18日" }
```

因为在这套 MVU 语义里，`/时间系统/日期` 已经是在 `stat_data` 内部。

### 6.4 速查表

| 场景 | 写法 | 解释 |
|---|---|---|
| EJS 读取 | `getvar('stat_data.时间系统.日期')` | 点路径，从变量树读 |
| EJS 写入 | `setvar('stat_data.时间系统.日期', value)` | 点路径，写变量树 |
| Tavern Helper 宏 | `{{format_message_variable::stat_data}}` | 格式化楼层变量 |
| MVU 更新 | `"/时间系统/日期"` | JSON Pointer，相对 `stat_data` |
| 世界书标题 | `[InitialVariables]` | 初始变量树 |

---

## 7. 结合 `性斗学园` 角色卡的实操示例

### 7.1 查看角色卡结构

用 Node 直接查看角色卡：

```powershell
cd D:\codesaves\SillyTavern
node -e "const fs=require('fs'); const p='preset_and_chara/chara/性斗学园/性斗学园超级重制版current.json'; const d=JSON.parse(fs.readFileSync(p,'utf8')); const data=d.data||d; console.log(data.name); console.log(Object.keys(data.extensions)); console.log(data.character_book.entries.length);"
```

预期会看到：

```text
性斗学园超级重制版
[ 'talkativeness', 'fav', 'world', 'depth_prompt', 'regex_scripts', 'tavern_helper', 'xiaobaix-tasks' ]
218
```

这说明：

- 角色卡名是 `性斗学园超级重制版`。
- 复杂逻辑主要挂在 `extensions`。
- 内嵌世界书有大量条目。

### 7.2 示例一：魅力阶段控制器

角色卡中的 `EJS魅力阶段控制器` 逻辑可以简化为：

```ejs
<魅力值>
<%
const charmValue = Number(getvar('stat_data.核心状态._魅力')) || 0;
%>

<% if (charmValue >= 200) { %>
魅力阶段: SS级 (200+)
NPC 对你表现出极强好感，社交判定几乎总是成功。
<% } else if (charmValue >= 80) { %>
魅力阶段: A级
多数普通 NPC 会更愿意与你对话。
<% } else { %>
魅力阶段: 普通
社交影响按常规规则处理。
<% } %>
</魅力值>
```

你可以新建一个世界书条目测试：

```text
标题：EJS测试_魅力阶段
常驻：开
位置：after_char
内容：
<%
const charm = Number(getvar('stat_data.核心状态._魅力', { defaults: 0 }));
%>
当前魅力为 <%- charm %>。
<% if (charm >= 50) { %>
本轮允许出现更积极的社交反馈。
<% } else { %>
本轮社交反馈保持普通。
<% } %>
```

演示方式：

1. 打开任意聊天。
2. 确认 `ST-Prompt-Template` 启用。
3. 把变量设为不同值。
4. 发送消息并用提示词查看器检查最终 prompt。

如果你没有 UI 变量编辑入口，可以临时在消息中让模板写变量：

```ejs
<% setvar('stat_data.核心状态._魅力', 80) -%>
```

注意：这种写法适合测试，不建议长期留在会发送给模型的正文里。

### 7.3 示例二：地图控制器

`EJS地图控制器` 的思路是：

1. 读取当前位置：

```javascript
const currentLocName = getvar('stat_data.位置系统.地点名称') || '';
```

2. 准备映射表：

```javascript
const locationMap = [
    { target: 'D/C班基础教室', triggers: ['D/C班基础教室', '第七教学楼'] },
    { target: 'A/B班进阶教室', triggers: ['A/B班进阶教室', '第七教学楼二层'] },
];
```

3. 找到匹配地点后输出对应规则：

```ejs
<%
const currentLocName = getvar('stat_data.位置系统.地点名称', { defaults: '' });
const locationMap = [
    { target: 'D/C班基础教室', triggers: ['D/C班基础教室', '第七教学楼'] },
    { target: 'A/B班进阶教室', triggers: ['A/B班进阶教室', '第七教学楼二层'] },
];
const hit = locationMap.find(item => item.triggers.includes(currentLocName));
%>

<% if (hit) { %>
当前位置命中：<%- hit.target %>
<%- await getwi(hit.target) %>
<% } else { %>
当前位置未命中专属地图条目，按普通场景处理。
<% } %>
```

这个模式适合所有地图类角色卡：

```text
变量保存当前位置 -> EJS 查映射 -> getwi 拉取地点条目 -> 注入地点规则
```

### 7.4 示例三：比赛控制器

`EJS比赛控制器` 读取日期、星期和地点：

```javascript
const weekDay = getvar('stat_data.时间系统.星期') || 0;
const currentDate = getvar('stat_data.时间系统.日期') || '';
const locationName = getvar('stat_data.位置系统.地点名称') || '';
```

简化示例：

```ejs
<%
const weekDay = Number(getvar('stat_data.时间系统.星期', { defaults: 1 }));
const currentDate = getvar('stat_data.时间系统.日期', { defaults: '' });
const locationName = getvar('stat_data.位置系统.地点名称', { defaults: '' });

let eventName = '';
if (currentDate.includes('6月9日') || currentDate.includes('06-09')) {
    eventName = '多校联赛';
} else if (weekDay === 6 || weekDay === 7) {
    eventName = '周末校内赛';
} else if (locationName.includes('擂台')) {
    eventName = '擂台赛';
}
%>

<% if (eventName) { %>
当前比赛事件：<%- eventName %>
<%- await getwi(eventName) %>
<% } %>
```

这个写法的优点：

- 模型不需要自己猜今天有没有比赛。
- 事件是否发生由变量和规则决定。
- 规则条目可以拆开维护。

### 7.5 示例四：在场人物控制器

`EJS角色控制器` 的核心是读取在场人物：

```javascript
let currentPresent = getvar('stat_data.关系系统.在场人物') || [];
```

简化可抄版：

```ejs
<%
const present = getvar('stat_data.关系系统.在场人物', { defaults: [] });
%>

<% for (const name of present) { %>
<%- await getwi(name) %>
<% } %>
```

更安全一点：

```ejs
<%
const present = getvar('stat_data.关系系统.在场人物', { defaults: [] });
const uniqueNames = [...new Set(Array.isArray(present) ? present : [])];
%>

<% for (const name of uniqueNames) { %>
<% if (typeof name === 'string' && name.trim()) { %>
<%- await getwi(name.trim()) %>
<% } %>
<% } %>
```

适用场景：

- 队伍成员。
- 同场 NPC。
- 地点内角色。
- 后台并行事件参与者。

### 7.6 示例五：MVU 输出格式

`性斗学园` 的世界书里有 `[mvu_update]变量输出格式` 和 `[mvu_update]变量输出格式强调`。

它要求模型在回复末尾输出：

```xml
<UpdateVariable>
...
</UpdateVariable>
```

如果使用 JSONPatch 风格，常见结构是：

```xml
<UpdateVariable>
<JSONPatch>
[
  { "op": "replace", "path": "/时间系统/日期", "value": "2025年03月18日" },
  { "op": "replace", "path": "/位置系统/地点名称", "value": "D/C班基础教室" },
  { "op": "delta", "path": "/核心状态/_魅力", "value": 1 }
]
</JSONPatch>
</UpdateVariable>
```

流程是：

```text
模型输出 UpdateVariable
  -> MVU 解析
  -> 更新 message/local 变量
  -> 下一轮 getvar('stat_data.xxx') 读到新状态
  -> EJS 控制器按新状态注入规则
```

---

## 8. 结合角色卡的外部 UI 演示

`性斗学园超级重制版current.json` 的 `regex_scripts` 中有一组 UI 替换脚本。

本地版本示例：

```html
<body>
  <script>
    $('body').load('http://localhost:5500/dist/性斗学园/正文UI/index.html')
  </script>
</body>
```

远程 CDN 版本示例：

```html
<body>
<script>
  $('body').load('https://testingcf.jsdelivr.net/gh/vincentrong2005/Fatria/dist/性斗学园/正文UI/index.html')
</script>
</body>
```

它的机制通常是：

1. 模型输出特定标签，例如 `<content>...</content>`、`<parallel>...</parallel>`、`<option>...</option>`。
2. SillyTavern 正则扩展匹配这些标签。
3. 正则把原始文本替换成 HTML/iframe/script。
4. HTML 再从本地或远程加载 UI。

这和 prompt 注入不同：

- prompt 注入影响发给模型的内容。
- 正则 UI 替换影响页面显示。
- UI 替换不一定改变原始消息内容。

最小可演示 UI：

1. 准备本地文件：

```text
D:\codesaves\demo-ui\dist\demo\index.html
```

内容：

```html
<div style="padding: 12px; border: 1px solid #999;">
  <strong>Demo UI</strong>
  <div id="msg">外部 HTML 已加载</div>
</div>
```

2. 启动静态服务器：

```powershell
cd D:\codesaves\demo-ui
npx http-server . -p 5500 --cors
```

3. 在正则替换结果里写：

```html
<body>
  <script>
    $('body').load('http://localhost:5500/dist/demo/index.html')
  </script>
</body>
```

4. 触发对应正则，聊天楼层中应显示 `Demo UI`。

---

## 9. 原生语法、插件语法、模型标签的区别

### 9.1 SillyTavern 原生宏

常见：

```text
{{user}}
{{char}}
{{lastMessage}}
{{lastMessageId}}
```

用途：

- 简单文本替换。
- 不执行复杂 JS。
- 适合角色名、用户代称、少量动态字段。

### 9.2 Slash 命令

常见形式：

```text
/echo severity=success Hello
/pass {{lastMessageId}}
/trigger
```

在 Tavern Helper 中可以用：

```javascript
await triggerSlash('/echo severity=success 运行成功');
```

### 9.3 EJS 模板

常见：

```ejs
<% const x = 1; %>
<%- x %>
<% if (x > 0) { %>
正数
<% } %>
```

用途：

- 生成前处理 prompt。
- 渲染后处理消息。
- 按变量、日期、地点、角色关系决定插入内容。

### 9.4 世界书注入标签

常见：

```text
[GENERATE:BEFORE]
[RENDER:AFTER]
@INJECT pos=0,role=system
```

用途：

- 控制条目位置。
- 控制内容是否发送给模型。
- 控制是否作为独立 message 注入。

### 9.5 模型输出标签

角色卡常让模型输出：

```xml
<content>正文</content>
<parallel>并行事件</parallel>
<option>选项</option>
<UpdateVariable>变量更新</UpdateVariable>
```

这些不是 SillyTavern 原生语法，而是角色卡约定。后续可能由：

- 正则扩展隐藏或渲染。
- MVU 解析变量更新。
- 外部 UI 脚本读取并显示。

---

## 10. 从零做一个可演示的小系统

目标：做一个最小“地点 + 魅力 + 事件”的系统。

### 10.1 初始化变量条目

新建世界书条目：

```text
标题：
[InitialVariables]

内容：
{
  "stat_data": {
    "时间系统": {
      "日期": "2025年03月17日",
      "星期": 1
    },
    "位置系统": {
      "地点名称": "D/C班基础教室"
    },
    "核心状态": {
      "_魅力": 10,
      "_幸运": 10
    },
    "关系系统": {
      "在场人物": ["夏洛特"]
    }
  }
}
```

注意：

- 内容必须是合法 JSON。
- `[InitialVariables]` 是否生效取决于插件设置和立即加载世界书设置。
- 如果你用 MVU，也可以用 MVU 的 `[initvar]` 机制，但不要和 `[InitialVariables]` 混淆。

### 10.2 地点条目

新建世界书条目：

```text
标题：
D/C班基础教室

内容：
地点规则：这里是普通教室。允许发生日常对话、小型冲突和课程事件。不得直接触发高风险战斗。
```

### 10.3 角色条目

新建世界书条目：

```text
标题：
夏洛特

内容：
夏洛特在场。她外表高傲，但在被真诚对待时会短暂放下戒备。若好感度不足，不应主动透露核心秘密。
```

### 10.4 控制器条目

新建常驻世界书条目：

```text
标题：
EJS测试控制器

内容：
<%
const loc = getvar('stat_data.位置系统.地点名称', { defaults: '' });
const present = getvar('stat_data.关系系统.在场人物', { defaults: [] });
const charm = Number(getvar('stat_data.核心状态._魅力', { defaults: 0 }));
%>

<controller_state>
当前位置：<%- loc %>
当前魅力：<%- charm %>
在场人物：<%- Array.isArray(present) ? present.join('、') : '' %>
</controller_state>

<% if (loc) { %>
<%- await getwi(loc) %>
<% } %>

<% for (const name of (Array.isArray(present) ? present : [])) { %>
<%- await getwi(name) %>
<% } %>

<% if (charm >= 50) { %>
额外规则：玩家当前魅力较高，NPC 更愿意给出正面反馈。
<% } %>
```

### 10.5 让模型更新变量

在输出格式条目中要求模型末尾追加：

```xml
<UpdateVariable>
<JSONPatch>
[
  { "op": "replace", "path": "/位置系统/地点名称", "value": "D/C班基础教室" },
  { "op": "delta", "path": "/核心状态/_魅力", "value": 1 }
]
</JSONPatch>
</UpdateVariable>
```

如果没有 MVU，这段只会作为文本存在；如果安装并启用 MVU，它会被解析为变量更新。

### 10.6 演示输入

在聊天中输入：

```text
我走进第七教学楼，尝试和夏洛特打招呼。
```

预期机制：

```text
用户输入
  -> 世界书扫描
  -> EJS测试控制器常驻进入
  -> 控制器读取 stat_data
  -> getwi('D/C班基础教室')
  -> getwi('夏洛特')
  -> 模型收到地点规则和角色规则
  -> 模型回复
  -> MVU 解析 UpdateVariable
  -> 下一轮变量变化
```

---

## 11. 调试方法

### 11.1 浏览器控制台

按 `F12` 打开开发者工具，看 Console。

重点看：

- 外部脚本是否加载成功。
- `import` 是否 CORS 报错。
- EJS 是否抛异常。
- MVU 是否解析失败。
- 正则替换是否报错。

常见报错：

```text
Failed to fetch dynamically imported module
CORS policy
Unexpected token
ReferenceError: getvar is not defined
```

含义：

| 报错 | 常见原因 |
|---|---|
| `Failed to fetch dynamically imported module` | 远程 JS 地址不可访问或 MIME 类型不对 |
| `CORS policy` | 本地服务器没开 CORS 或远程资源禁止跨域 |
| `Unexpected token` | JS/EJS 语法错误 |
| `getvar is not defined` | 在错误环境执行了 Prompt Template API |

### 11.2 检查最终 prompt

建议启用提示词查看器或 Prompt Manager 的 prompt inspection 功能。

你要确认：

- EJS 是否被执行，而不是原样发给模型。
- `getwi(...)` 拉取的条目是否出现。
- `@INJECT` 是否成为独立 message。
- `{{format_message_variable::stat_data}}` 是否展开。
- `<UpdateVariable>` 是否被隐藏、保留或发送给模型，符合你的设计。

### 11.3 检查角色卡 JSON

列出角色卡里的世界书条目标题：

```powershell
node -e "const fs=require('fs'); const p='preset_and_chara/chara/性斗学园/性斗学园超级重制版current.json'; const d=JSON.parse(fs.readFileSync(p,'utf8')); const entries=(d.data||d).character_book.entries; for (const e of entries.slice(0,30)) console.log(e.comment);"
```

搜索 EJS 条目：

```powershell
node -e "const fs=require('fs'); const p='preset_and_chara/chara/性斗学园/性斗学园超级重制版current.json'; const d=JSON.parse(fs.readFileSync(p,'utf8')); const entries=(d.data||d).character_book.entries; for (const e of entries) if ((e.comment+' '+e.content).includes('<%')) console.log(e.comment);"
```

搜索外部脚本：

```powershell
node -e "const fs=require('fs'); const p='preset_and_chara/chara/性斗学园/性斗学园超级重制版current.json'; const d=JSON.parse(fs.readFileSync(p,'utf8')); const scripts=(d.data||d).extensions.tavern_helper.scripts; for (const s of scripts) console.log(s.name || '(unnamed)', '\\n', s.content, '\\n');"
```

### 11.4 检查本地静态服务器

如果角色脚本导入：

```javascript
import 'http://localhost:5500/dist/性斗学园/性斗学园脚本/index.js'
```

先用浏览器或 PowerShell 测：

```powershell
Invoke-WebRequest 'http://localhost:5500/dist/性斗学园/性斗学园脚本/index.js' -UseBasicParsing |
    Select-Object StatusCode,Content
```

如果 `StatusCode` 不是 `200`，酒馆助手也加载不了。

---

## 12. 常见坑

### 12.1 把 `getwi` 和原生世界书激活混为一谈

错误理解：

```text
getwi 会触发世界书关键词规则。
```

更准确：

```text
getwi 是直接取条目内容；activewi 才更接近交给世界书激活流程。
```

### 12.2 把 MVU 路径写成 `/stat_data/...`

在本套 MVU 用法里，JSONPatch 路径相对 `stat_data`。

正确：

```json
{ "op": "replace", "path": "/时间系统/日期", "value": "2025年03月18日" }
```

错误：

```json
{ "op": "replace", "path": "/stat_data/时间系统/日期", "value": "2025年03月18日" }
```

### 12.3 在普通外部 JS 里直接用 EJS API

`getvar(...)` 是 Prompt Template 模板上下文中的函数。外部 JS 脚本里能不能直接用，取决于 Tavern Helper 暴露了哪些 API。

外部 JS 中更稳妥的是使用 Tavern Helper 的接口，例如：

```javascript
const vars = await TavernHelper.getVariables?.();
```

或通过 Slash 命令间接读写：

```javascript
await triggerSlash('/setvar key=demo value=123');
```

### 12.4 远程脚本没有固定版本

这种地址可能随仓库更新改变行为：

```text
https://cdn.jsdelivr.net/gh/user/repo/dist/index.js
```

更稳定的是带 tag / commit：

```text
https://cdn.jsdelivr.net/gh/user/repo@v1.2.3/dist/index.js
```

`性斗学园` 中有些脚本使用 `@beta`，意味着功能可能变化：

```text
https://testingcf.jsdelivr.net/gh/MagicalAstrogy/MagVarUpdate@beta/artifact/bundle.js
```

适合跟随新功能，但不适合追求长期稳定。

### 12.5 把 UI 替换当成 prompt 规则

正则替换加载 UI：

```html
$('body').load('https://.../正文UI/index.html')
```

这通常只影响显示，不等于模型看到了这些 UI 内容。模型看到什么，要看最终 prompt。

---

## 13. 推荐工作流

### 13.1 读卡

先看：

```text
data.character_book.entries
data.extensions.tavern_helper.scripts
data.extensions.regex_scripts
```

不要只看 `description`。

### 13.2 改卡

建议顺序：

1. 先改世界书条目，保证 prompt 逻辑正确。
2. 再改变量初始化和 MVU 更新。
3. 再改 UI 正则替换。
4. 最后再接远程脚本或本地脚本。

### 13.3 调试

每改一层就验证：

| 层 | 验证方法 |
|---|---|
| 世界书 | 提示词查看器中确认条目出现 |
| EJS | 看最终 prompt 是否已替换 |
| 变量 | 输出 `{{format_message_variable::stat_data}}` 或临时 `<%- getvar(...) %>` |
| MVU | 看 `<UpdateVariable>` 是否被解析，下一轮变量是否变化 |
| 外部 JS | 浏览器 Console 和 Network |
| UI | 检查正则替换后的楼层 HTML |

### 13.4 自己写复杂卡的分工建议

| 需求 | 放在哪里 |
|---|---|
| 世界观固定设定 | 普通世界书条目 |
| 根据变量选择规则 | EJS 控制器 |
| 状态持久化 | MVU / 变量系统 |
| 复杂状态校验 | 外部 JS 或 EJS 控制器 |
| 漂亮状态栏 | 正则替换 + 外部 HTML |
| 自动更新角色卡 | Tavern Helper 外部脚本 |
| 强制 prompt 结构 | `@INJECT` |

---

## 14. 最小速查

```powershell
# 启动 SillyTavern
cd D:\codesaves\SillyTavern
npm start
```

```javascript
// Tavern Helper：导入外部脚本
import 'https://cdn.jsdelivr.net/gh/user/repo@version/dist/index.js'
```

```javascript
// Tavern Helper：运行 Slash 命令
await triggerSlash('/echo severity=success Hello');
```

```ejs
<% /* Prompt Template：执行 JS */ %>
<%- getvar('stat_data.核心状态._魅力', { defaults: 0 }) %>
```

```ejs
<% if (getvar('stat_data.核心状态._魅力', { defaults: 0 }) >= 80) { %>
<%- await getwi('高魅力规则') %>
<% } %>
```

```text
# 世界书标题：发送前注入
[GENERATE:BEFORE]
```

```text
# 世界书标题：独立 system message
@INJECT pos=0,role=system
```

```xml
<!-- MVU：更新 stat_data 内变量 -->
<UpdateVariable>
<JSONPatch>
[
  { "op": "replace", "path": "/时间系统/日期", "value": "2025年03月18日" },
  { "op": "delta", "path": "/核心状态/_魅力", "value": 1 }
]
</JSONPatch>
</UpdateVariable>
```

```text
# Tavern Helper 变量宏：把 stat_data 展开进 prompt
{{format_message_variable::stat_data}}
```

---

## 15. 本仓库里最值得继续读的文件

项目核心：

```text
explain.md
project-memory.md
public/script.js
public/scripts/world-info.js
public/scripts/openai.js
public/scripts/PromptManager.js
src/prompt-converters.js
src/endpoints/backends/chat-completions.js
```

插件文档：

```text
data/default-user/extensions/JS-Slash-Runner/README.md
data/default-user/extensions/JS-Slash-Runner/@types/function
data/default-user/extensions/ST-Prompt-Template/README_CN.md
data/default-user/extensions/ST-Prompt-Template/docs/features_cn.md
data/default-user/extensions/ST-Prompt-Template/docs/reference_cn.md
```

角色卡样例：

```text
preset_and_chara/chara/性斗学园/性斗学园超级重制版current.json
preset_and_chara/chara/MoST/MoST-2-7-7.json
preset_and_chara/chara/命定之诗与黄昏之歌/命定之诗与黄昏之歌v4.2.json
preset_and_chara/chara/欲望世界/欲望世界0407_extract_v3.json
```

已有专题手册：

```text
酒馆助手与提示词模板使用手册.md
```

如果你只想最快掌握这套复杂卡的机制，阅读顺序建议是：

```text
project-memory.md
  -> 酒馆助手与提示词模板使用手册.md
  -> preset_and_chara/chara/性斗学园/性斗学园超级重制版current.json
  -> data/default-user/extensions/ST-Prompt-Template/docs/reference_cn.md
```
