# 酒馆助手与提示词模板使用手册

## 适用范围
这份手册面向两类人：

1. 已经会用 SillyTavern 原生世界书，但不清楚“外置 JS + 插件 + EJS 控制器”如何配合的人。
2. 想参考 `MoST-2-7-7` / `性斗学园超级重制版` 这类角色卡，自己写动态世界书系统的人。

本文会把下面几层东西彻底拆开：
- SillyTavern 原生世界书
- 酒馆助手（Tavern Helper / JS-Slash-Runner）
- 提示词模板（ST-Prompt-Template）
- MVU（MagVarUpdate）
- 角色卡里导入的远程脚本
- `MoST-2-7-7` 中的 EJS 控制器

核心目标只有一个：

> 搞清楚“世界书条目到底是谁在决定导入、何时导入、导入到哪里、导入后又是谁继续处理”。

---

## 一、先给结论：这套系统的真正分工
如果只看原生 SillyTavern，世界书逻辑确实很简单：
- 关键字命中
- 条目激活
- 按 `before_char` / `after_char` 注入

但 `MoST-2-7-7` 这种角色卡已经不是只靠这套逻辑了。

它的实际链路是：

1. **SillyTavern 原生世界书**  
   负责基础装配：把启用条目分到 `before_char` / `after_char`。

2. **提示词模板（ST-Prompt-Template）**  
   负责让世界书条目变成“可执行模板”，支持：
   - `getvar(...)`
   - `getwi(...)`
   - `activewi(...)`
   - `<% ... %>` / `<%- ... %>` / `print(...)`

3. **酒馆助手（Tavern Helper）**  
   负责运行角色卡外挂 JS 脚本，并给脚本提供：
   - 事件系统
   - 脚本按钮
   - 脚本作用域
   - 共享全局接口
   - 访问 SillyTavern、EjsTemplate、Mvu 的桥接

4. **MVU（MagVarUpdate）**  
   负责变量更新框架：
   - 解析 `<UpdateVariable>`
   - 更新 `stat_data`
   - 额外模型解析
   - 给别的脚本提供 `window.Mvu`

5. **角色卡附带脚本（如 性斗学园脚本）**  
   负责“变量更新之后”的二次计算与 UI，不负责世界书导入本身。

所以真正的控制关系是：

> 原生世界书负责“先把控制器条目送进 prompt”；提示词模板负责“执行控制器”；酒馆助手负责“运行外挂脚本”；MVU 负责“维护变量状态”；状态栏脚本负责“消费变量做 UI 和后处理”。

---

## 二、插件和脚本之间的关系

## 2.1 酒馆助手到底是什么
官方文档里，酒馆助手是 `JS-Slash-Runner` 的文档体系中的一部分，英文名通常写作 **Tavern Helper**。

它的官方定位不是“单一功能插件”，而是一个脚本运行平台。官方介绍强调它能：
- 渲染交互式前端界面
- 深度访问 SillyTavern 自身
- 监听酒馆事件
- 连接外部应用
- 让角色卡获得更好的 UI 和脚本能力

你可以把它理解为：

> “给 SillyTavern 加了一个可运行前端 JS 的外挂层，并封装了一批脚本作者需要的接口。”

这也是为什么很多角色卡不会要求你安装一整个传统扩展，而是让你在“角色脚本”里写一行：

```javascript
import 'https://testingcf.jsdelivr.net/gh/某仓库/dist/某脚本/index.js'
```

这不是 EJS，也不是世界书本身，而是 **酒馆助手脚本**。

---

## 2.2 提示词模板到底是什么
提示词模板（`ST-Prompt-Template`）的官方定位是：

> “让你能在 prompt、角色卡、世界书、消息中使用完整 JavaScript / EJS 模板语法。”

它主要增强的是“提示词拼装阶段”。

它能做的关键事情有：
- 在发送给 LLM 前执行 `<% ... %>`
- 在消息渲染后再执行可见模板
- 提供 `getvar`, `setvar`, `getwi`, `activewi`, `getchar` 等 API
- 支持 `[GENERATE:*]`、`[RENDER:*]`、`@INJECT`
- 支持立即加载世界书、世界书预处理、注入提示词等

因此：
- **酒馆助手** 主要解决“运行外挂 JS 脚本”
- **提示词模板** 主要解决“让世界书 / prompt 本身变成可执行模板”

这两者不是同一个层面。

---

## 2.3 MVU 到底是什么
MVU（`MagVarUpdate`）官方 README 的定位很直接：

> 基于酒馆助手的一个脚本，用于提供基于变量的状态维护，并减少状态栏对模型注意力的占用。

所以它不是世界书引擎，也不是 prompt 引擎，而是：
- 一个 **变量状态框架**
- 一个 **消息后处理框架**
- 一个 **额外模型解析框架**

它的角色是：
- 读取模型输出中的变量更新段
- 解析成结构化变更
- 写回聊天变量
- 对外暴露 `Mvu` 接口

在这张卡里，MVU 是“状态机核心”，但不是“世界书控制器核心”。

---

## 2.4 StageDog / Fatria 这些仓库又是什么
这类仓库通常不是“插件本体”，而是：
- 酒馆助手脚本仓库
- 前端界面 / 工具脚本仓库
- 用 jsDelivr 做自动更新发布的资源仓库

例如 `StageDog/tavern_resource` 官方作品集里明确写了：
- 很多脚本都可以直接复制 `import 'xxx'`
- 几乎都靠这种 `import` 形式自动更新

这意味着：

> 角色卡作者可以把逻辑写在自己的 GitHub 仓库里，再通过酒馆助手脚本导入到角色卡环境中。

这样就实现了：
- 脚本和角色卡分离
- 无需用户手动替换本地文件
- 更新时只要远程仓库变了，脚本就能自动跟新

---

## 三、三套“世界书导入”机制的区别
这是最容易混淆的地方。

## 3.1 原生酒馆导入
这是你已经熟悉的那套：
- 条目启用
- 关键字或 constant 激活
- 按 `before_char` / `after_char` 注入

特点：
- 简单
- 稳定
- 可视化编辑友好
- 但条件逻辑有限

---

## 3.2 `getwi(...)` 直接导入
这是提示词模板提供的能力。

官方文档对 `getwi` 的说明非常关键：
- 它是最直接的加载世界书方式
- **完全绕过酒馆内置的世界书处理逻辑**
- 可以无条件激活
- 可以精确控制位置和条件
- 可以多次调用
- 可以激活未挂载的世界书条目

这意味着：

> `getwi` 不是“帮你模拟一次原生关键词触发”，而是“直接把指定条目的内容拉进当前 prompt 片段”。

这就是 `MoST-2-7-7` 中 EJS 控制器真正依赖的机制。

例如：

```javascript
<% if (getvar('stat_data.核心状态._魅力') > 50) { %>
<%- await getwi('高魅力阶段规则') %>
<% } %>
```

这个时候不是酒馆在扫关键字，而是模板执行器在主动读某个条目内容。

---

## 3.3 `activewi(...)` 原生式激活
提示词模板还提供了 `activewi(...)`。

官方文档说明：
- 如果你需要原生 🟢 关键字激活逻辑
- 可以用 `activewi` 把某条世界书加入待激活列表
- 然后再由酒馆走它自己的原生激活逻辑

所以：

- `getwi(...)`：直接取内容，绕过原生世界书逻辑
- `activewi(...)`：仍然交还给原生世界书逻辑处理

而 `MoST-2-7-7` 当前卡的控制器主力是 `getwi(...)`，不是 `activewi(...)`。

---

## 四、提示词模板里的世界书，不只是“条目内容”
提示词模板把世界书扩展成了好几种不同用途。

## 4.1 普通世界书条目
就是最传统的：
- 被原生世界书逻辑扫描
- 激活后拼到上下文里

---

## 4.2 EJS 控制器条目
这类条目通常本身是 `constant: true`，或者能稳定进入 prompt，然后里面写：

```javascript
<% ... %>
```

作用不是直接提供世界观，而是：
- 读变量
- 判断条件
- 再去拉别的条目

这就是：
- `EJS大型事件控制器`
- `EJS角色控制器`
- `EJS地图控制器`
- `EJS比赛控制器`

这类条目本质上是：

> “世界书里的调度脚本”

---

## 4.3 `[GENERATE:*]` / `[RENDER:*]` 条目
这是提示词模板官方支持的“内容注入”能力。

例如：
- `[GENERATE:BEFORE]`
- `[GENERATE:AFTER]`
- `[RENDER:BEFORE]`
- `[RENDER:AFTER]`

用途是：
- 在生成阶段注入
- 在消息渲染阶段注入

它和 `getwi` 的区别是：
- `[GENERATE:*]` 更像“声明式注入”
- `getwi` 更像“脚本式导入”

---

## 4.4 `@INJECT` 条目
这是提示词模板官方支持的 Prompt 注入能力。

官方文档强调：
- 这是精细控制消息插入位置的功能
- 支持绝对位置、相对目标、正则匹配
- 实际上是在构造一条独立消息，而不是把世界书简单并成大文本

例如：

```text
@INJECT pos=0,role=system
```

或：

```text
@INJECT target=user,at=after,role=assistant
```

它适合做：
- 独立 system 指令
- 强格式化 prompt
- 把 CoT / 元指令和知识条目分离

但 `MoST-2-7-7` 当前主逻辑核心不是 `@INJECT`，而是 `getwi(...)` + EJS 控制器。

---

## 五、酒馆助手脚本到底怎么参与这张角色卡
这一层和提示词模板完全不同。

提示词模板写在世界书里。  
酒馆助手脚本写在 **角色脚本 / 预设脚本 / 全局脚本** 里。

官方文档定义了脚本对象结构，大致包含：
- `enabled`
- `name`
- `id`
- `content`
- `info`
- `button.buttons`
- `data`

也就是说，一条酒馆助手脚本不只是“代码文本”，还可以带：
- 可显示按钮
- 作者说明
- 配置数据

这也是为什么 `MoST-2-7-7` 里这些脚本能自带按钮和配置：
- `MVU`
- `自动更新世界书`
- `性斗学园脚本`

---

## 六、这张卡里 5 个启用脚本分别做什么
下面按 `MoST-2-7-7` 当前启用状态来解释。

## 6.1 世界书强制自定义排序
脚本内容：

```javascript
import 'https://testingcf.jsdelivr.net/gh/StageDog/tavern_resource/dist/酒馆助手/世界书强制自定义排序/index.js'
```

本地抓到的实际行为非常简单：
- 把 `#world_info_sort_order` 改成 `13`
- 触发 change
- 禁用这个控件

它不直接导入世界书条目，也不读变量。

它真正的作用是：
- 强制作者推荐的世界书排序模式
- 防止编辑器切换排序方式后，人工维护的 `insertion_order` 失去意义

因此它是 **顺序保护脚本**，不是调度脚本。

---

## 6.2 MVU
脚本内容：

```javascript
import 'https://testingcf.jsdelivr.net/gh/MagicalAstrogy/MagVarUpdate@beta/artifact/bundle.js'
```

它是这张卡最重要的外置脚本之一。

本地源码和官方 README 一起看，可以总结成：

### 它会做什么
- 在扩展区挂载自己的配置面板
- 提供变量更新按钮
- 监听生成完成
- 解析 `<UpdateVariable>`
- 更新变量
- 挂出全局 `Mvu`
- 发出变量更新事件

### 它自带什么按钮
当前卡里启用的按钮有：
- `重新处理变量`
- `重新读取初始变量`
- `清除旧楼层变量`
- `快照楼层`
- `重演楼层`
- `重试额外模型解析`

### 它为什么重要
因为这张卡不是让模型“只写故事”，而是要求模型每轮都输出状态更新。  
MVU 就是负责把这部分输出吃回系统。

---

## 6.3 Zod 变量结构
脚本内容是内联脚本，核心是：

```javascript
import { registerMvuSchema } from '.../mvu_zod.js';
...
registerMvuSchema(Schema);
```

这不是世界书调度脚本，也不是剧情脚本。

它的职责只有一个：

> 给 MVU 注册一份严格的 `stat_data` schema。

作用包括：
- 约束变量结构
- 给字段设置默认值
- 限制数值范围
- 修正不合法 patch

所以它是 **变量合法性护栏**。

---

## 6.4 自动更新世界书 / 角色卡
脚本内容：

```javascript
import 'https://testingcf.jsdelivr.net/gh/StageDog/tavern_resource/dist/酒馆助手/自动更新角色卡/index.js'
```

它带了这几个数据字段：
- `角色卡名称`
- `角色卡链接`
- `更新日志链接`

它的用途是：
- 给玩家提供“直接在酒馆里更新角色卡”的按钮
- 不参与 prompt 拼装
- 不参与动态世界书导入

它属于 **分发与更新层**。

---

## 6.5 性斗学园脚本
脚本内容：

```javascript
import 'https://cdn.jsdelivr.net/gh/vincentrong2005/Fatria/dist/性斗学园/性斗学园脚本/index.js'
```

这是第二个关键脚本，但它不是世界书控制器。

### 它实际做什么
本地源码显示，它会：
- 等待 `Mvu` 初始化
- 读取 `Mvu.getMvuData(...)`
- 调用 `Mvu.replaceMvuData(...)`
- 监听 `Mvu.events.VARIABLE_UPDATE_ENDED`
- 规范化关系系统名字
- 规范化 `在场人物`
- 重算段位与派生属性
- 挂一个 `打开状态栏` 按钮

### 它不做什么
它并不直接负责：
- 选择哪条世界书进入 prompt
- 执行 `getwi(...)`
- 决定哪位角色条目被导入

所以它是 **变量后处理 / UI 脚本**，不是世界书编排器。

---

## 七、酒馆助手脚本是如何互相协作的
酒馆助手官方文档里有两个非常重要的接口：

### 7.1 `initializeGlobal(name, value)`
把某个接口共享到全局。

### 7.2 `waitGlobalInitialized(name)`
等待别的脚本共享的全局接口可用。

官方示例甚至直接写了：

```javascript
initializeGlobal('Mvu', Mvu);
await waitGlobalInitialized('Mvu');
```

这就是脚本间协作的标准方式。

在这张卡的体系里，实际就形成了这条关系：

1. `MVU` 脚本先启动。
2. 它把 `Mvu` 共享到全局。
3. 其他脚本通过 `waitGlobalInitialized('Mvu')` 等待它完成初始化。
4. `性斗学园脚本`、`Zod 变量结构` 这种后续脚本再接管。

因此：

> 酒馆助手解决的不是“世界书导入”，而是“多个外挂脚本如何有序协作”。

---

## 八、脚本按钮和事件是怎么工作的
酒馆助手官方还提供了：
- `getScriptButtons()`
- `replaceScriptButtons()`
- `getButtonEvent(buttonName)`
- `eventOn(eventType, listener)`
- `eventEmit(...)`

这意味着脚本可以：
- 给自己添加按钮
- 动态隐藏/显示按钮
- 监听按钮点击
- 监听酒馆事件
- 自定义事件总线

例如官方推荐的按钮监听方式就是：

```javascript
const eventType = getButtonEvent('按钮名');
eventOn(eventType, () => {
  console.log('按钮被点击了');
});
```

在这张卡里：
- `MVU` 用按钮提供变量维护动作
- `性斗学园脚本` 提供 `打开状态栏`
- `自动更新角色卡` 可以提供更新入口

这层不改变世界书内容，但会改变“玩家如何和这套系统交互”。

---

## 九、`MoST-2-7-7` 里真正控制世界书导入的是谁
结论很明确：

> 真正控制世界书条目导入的是 **提示词模板里的 EJS 控制器**，不是酒馆助手脚本。

酒馆助手脚本负责的是：
- UI
- 变量框架
- 自动更新
- 脚本按钮
- 状态栏

而世界书条目到底进不进 prompt，真正起决定作用的是这些 EJS 条目：
- `EJS大型事件控制器`
- `EJS角色控制器`
- `EJS地图控制器`
- `EJS比赛控制器`
- `外校列表`
- `EJS魅力阶段控制器`
- `EJS幸运阶段控制器`
- `EJS堕落度阶段控制器`

这些都在世界书里，不在酒馆助手脚本里。

---

## 十、这张卡里的 EJS 控制器是怎样工作的

## 10.1 大型事件控制器
它读取：
- `stat_data.时间系统.日期`

然后按日期计算 `passedDays`，再执行：

```javascript
await getwi(null, activatedMainEvent)
await getwi(null, activatedPhaseEvent)
```

作用：
- 动态拉大型事件总条目
- 动态拉当前阶段条目

这里完全不是原生关键字扫描。

---

## 10.2 地图控制器
它读取：
- `stat_data.位置系统.地点名称`

再用地点映射表把“伪装名称 / 真实名称”统一到一个目标条目名，然后：

```javascript
await getwi(null, matchedLocation.target)
```

作用：
- 当前地点变了，局部场景条目就跟着变

---

## 10.3 比赛控制器
它读取：
- 日期
- 星期
- 地点

然后决定要导入：
- `多校联赛`
- `校内赛`
- `擂台赛`
- `黑暗决赛`

这是一种“按状态挑比赛模块”的逻辑。

---

## 10.4 角色控制器
这是整张卡里最强的控制器。

它同时参考：
- `在场人物`
- 主角姓名
- 用户最后一句输入中的角色名
- 时间门限制

最后把目标角色条目放进数组，再循环：

```javascript
for (const charName of charactersToLoad) {
  print(await getwi(null, `^${charName}$`));
}
```

这意味着：
- 不是只有“在场人物”能触发
- 玩家主动点名也可能触发
- 导入时按标题精确匹配
- 可以顺带拉隐藏事件条目

最典型的例子就是：
- `夏洛特好感事件`

它本身在 JSON 里是 `enabled: false`，但角色控制器仍可能通过 `getwi` 把它拉进 prompt。

这说明：

> 在这种体系里，“禁用条目”不一定是废条目，它可能只是“只允许脚本调度、禁止原生扫描碰到”的私有模块。

---

## 十一、`getwi` 为什么能拉禁用条目
提示词模板文档和源码给了关键证据。

官方参考里写明：
- `getEnabledWorldInfoEntries(...)` / 世界书集合读取时，**即使已禁用条目也会返回**
- `getwi` 递归导入依赖的是扩展自己的世界书读取逻辑
- `getwi` 会直接读取指定条目内容

所以：

> `getwi` 可以把“平时不让酒馆原生扫描命中”的条目，当作脚本私有资源来按需加载。

这正是动态模块化世界书的基础。

---

## 十二、原生世界书、`getwi`、酒馆助手脚本，到底该怎么分工
如果你自己要写一套类似系统，建议按下面方式分层。

## 12.1 原生世界书负责“稳定进入 prompt 的壳”
例如：
- 核心世界观
- 主线
- 地图总览
- 人物总表
- 变量列表
- 输出格式
- EJS 控制器本体

特点：
- 启用
- 顺序明确
- 能稳定送入 prompt

---

## 12.2 EJS 控制器负责“条件调度”
例如：
- 事件阶段切换
- 按地点导入地点条目
- 按在场人物导入角色条目
- 按数值区间输出阶段性规则

特点：
- 写在世界书里
- 使用 `getvar(...)` / `getwi(...)`
- 决定当前 prompt 的局部内容

---

## 12.3 酒馆助手脚本负责“外挂系统能力”
例如：
- 自动更新角色卡
- 自动安装其他脚本
- 状态栏 UI
- 事件监听
- 按钮交互
- 对外共享全局接口

特点：
- 写在脚本区，不写在世界书里
- 不直接决定世界书条目进不进 prompt
- 但会决定变量框架、交互方式和后处理方式

---

## 12.4 MVU 负责“状态闭环”
例如：
- 模型输出 `<UpdateVariable>`
- MVU 解析 patch
- 写回 `stat_data`
- 发出更新事件
- 状态栏脚本和下轮 EJS 都读取新状态

特点：
- 不负责剧情条目选择
- 负责让剧情选择和状态更新形成闭环

---

## 十三、给角色卡作者的推荐工作流
如果你要自己做一张类似的动态卡，推荐按下面顺序。

## 第 1 步：先用原生世界书把“静态骨架”搭起来
先不要上 JS，不要上 MVU。

至少先有：
- 核心世界观
- 地图总表
- 人物总表
- 主线
- 用户信息
- 输出格式

如果这层没整理好，后面全是噪音。

---

## 第 2 步：再加提示词模板
安装 `ST-Prompt-Template`，先用最小案例验证：

```javascript
当前日期：<%- getvar('stat_data.时间系统.日期') %>
```

然后再尝试：

```javascript
<%- await getwi('某个条目名') %>
```

先确认：
- EJS 能执行
- 变量能读到
- 条目能递归导入

---

## 第 3 步：把“导入逻辑”封装进控制器条目
不要把条件逻辑散落在几十个普通条目里。

正确做法是做几个专门控制器：
- 事件控制器
- 地点控制器
- 角色控制器
- 数值阶段控制器

这样世界书就会从“数据库”变成“模块仓库 + 调度器”。

---

## 第 4 步：再引入酒馆助手脚本
只有当你需要下面这些能力时，再引入酒馆助手：
- 状态栏 UI
- 自动更新
- 脚本按钮
- 前端界面
- 事件联动
- 脚本共享接口

不要把“世界书调度”和“UI 脚本”混在一个层面理解。

---

## 第 5 步：最后再接 MVU
只有当你确实需要：
- 可靠变量系统
- 独立状态栏
- 额外模型解析
- 多轮稳定状态闭环

再引入 MVU。

否则你会同时调三层系统，调试难度会爆炸。

---

## 十四、这张卡的真实调用链，用一句话概括
以 `MoST-2-7-7` 当前结构为例，真实链路是：

1. 原生世界书把 EJS 控制器和基础规则装进去。
2. 提示词模板执行控制器。
3. 控制器用 `getvar` 读状态、用 `getwi` 拉条目。
4. 主模型基于这份“动态裁切后的世界书快照”写出剧情和变量更新。
5. MVU 解析变量更新，写回 `stat_data`。
6. `性斗学园脚本` 监听 MVU 事件，做派生计算和状态栏刷新。
7. 下一轮控制器再读新的 `stat_data`，继续调度新的世界书条目。

所以这套系统本质上是：

> 世界书模块仓库 + EJS 调度器 + 变量状态机 + UI 后处理器。

---

## 十五、常见误区

### 误区 1：酒馆助手负责导入世界书
不对。

酒馆助手负责的是：
- 运行外挂 JS
- 挂按钮
- 发事件
- 共享接口

真正导入世界书条目的是：
- 原生世界书逻辑
- 或提示词模板里的 `getwi(...)`

---

### 误区 2：`getwi` 等于模拟关键字触发
不对。

`getwi` 是直接读取条目内容，绕过原生世界书处理逻辑。

---

### 误区 3：禁用条目就一定不会被用到
不对。

在提示词模板体系里：
- 禁用条目仍然可能被 `getwi(...)` 递归导入
- 它们常常被作者用作“私有模块”

---

### 误区 4：状态栏脚本就是世界书控制器
不对。

状态栏脚本通常只消费变量、监听更新事件、刷新 UI，不负责决定剧情条目导入。

---

## 十六、最值得你记住的 5 条实战原则

1. 原生世界书负责把“控制器”和“基础规则”稳定送进 prompt。
2. `getwi(...)` 才是动态导入条目的主武器，且它绕过原生逻辑。
3. 酒馆助手脚本负责脚本平台能力，不是世界书引擎。
4. MVU 负责变量闭环，不负责剧情条目选择。
5. 写复杂角色卡时，必须把“设定条目”“控制器条目”“变量框架”“UI 脚本”分层思考。

---

## 十七、这张卡里最推荐你优先继续读的内容
如果你想继续深入源码，推荐按这个顺序看：

1. `EJS角色控制器`
   - 因为它最能代表“动态人物条目导入”
2. `EJS地图控制器`
   - 因为它最能代表“地点映射式导入”
3. `EJS大型事件控制器`
   - 因为它最能代表“按日期切阶段”
4. `tmp_mvu_bundle.js`
   - 看 MVU 怎么组织变量更新
5. `tmp_xdxy_main.js`
   - 看状态栏脚本怎么接管 MVU 结果

---

## 官方来源
以下是本文主要依据的官方来源与项目页：

- 酒馆助手官方介绍  
  `https://n0vi028.github.io/JS-Slash-Runner-Doc/guide/关于酒馆助手/介绍.html`

- 酒馆助手：获取脚本 / 按钮 / 事件  
  `https://n0vi028.github.io/JS-Slash-Runner-Doc/guide/功能详情/酒馆助手脚本/获取脚本.html`

- 酒馆助手：监听和发送事件  
  `https://n0vi028.github.io/JS-Slash-Runner-Doc/guide/功能详情/监听和发送事件.html`

- 酒馆助手：分享接口（`initializeGlobal` / `waitGlobalInitialized`）  
  `https://n0vi028.github.io/JS-Slash-Runner-Doc/guide/功能详情/分享接口.html`

- 酒馆助手：访问 SillyTavern / EjsTemplate / Mvu  
  `https://n0vi028.github.io/JS-Slash-Runner-Doc/guide/功能详情/访问酒馆接口或其他插件.html`

- 提示词模板项目主页  
  `https://github.com/zonde306/ST-Prompt-Template`

- 提示词模板功能文档  
  `https://github.com/zonde306/ST-Prompt-Template/blob/main/docs/features.md`

- 提示词模板参考文档  
  `https://github.com/zonde306/ST-Prompt-Template/blob/main/docs/reference.md`

- MVU 项目主页  
  `https://github.com/MagicalAstrogy/MagVarUpdate`

- MVU 教程  
  `https://github.com/MagicalAstrogy/MagVarUpdate/blob/beta/doc/tutorial.md`

- StageDog 资源仓库  
  `https://github.com/StageDog/tavern_resource`

- StageDog 作品集（酒馆助手脚本说明）  
  `https://stagedog.github.io/青空莉/作品集/`

- 酒馆助手脚本模板仓库  
  `https://github.com/StageDog/tavern_helper_template`

---

## 十八、接口速查：你真正要写的东西

下面这部分不再讲宏观架构，只讲你写卡时真正会手敲的接口、语法和它们在 `MoST-2-7-7` 里的实际用途。

### 18.1 EJS 标签语法速查

这套卡的世界书控制器，本质上就是 EJS 模板。

| 写法 | 含义 | 是否输出文本 | 在这张卡里的典型用途 |
| --- | --- | --- | --- |
| `<% ... %>` | 执行 JavaScript | 否 | `if / else / for / const` |
| `<%- ... %>` | 输出表达式结果，原样输出 | 是 | `await getwi(...)` 的结果直接拼进 prompt |
| `<%= ... %>` | 输出表达式结果，但会做转义 | 是 | 提示词里很少用，当前卡几乎不用 |
| `<%_ ... _%>` | 执行代码，并尽量裁掉两侧空白 | 视内容而定 | 当前卡大量使用，避免控制器把空行塞进 prompt |
| `<%# ... %>` | EJS 注释 | 否 | 阶段控制器中的说明注释 |
| `print(...)` | 在代码块内部主动输出字符串 | 是 | 适合在复杂 `if` 内分段输出 |

几个最重要的点：

1. 当前插件把 EJS 编译成 `async` 函数，所以你可以直接写 `await getwi(...)`。
2. 当前卡大量使用 `<%_ ... _%>`，原因很简单：控制器里有很多条件判断，如果不用裁空白，prompt 会出现一堆无意义空行。
3. 当前卡导入条目几乎都用 `<%- await getwi(...) %>`，因为它需要把被读取到的世界书正文原样塞回 prompt。

最小例子：

```ejs
<%_
const date = getvar('stat_data.时间系统.日期') || '未知日期';
_%>
当前日期：<%- date %>
```

效果：

- `getvar(...)` 先读变量。
- `<%- date %>` 再把变量值真的输出到 prompt。

---

### 18.2 `getvar(...)` 一家：读变量

提示词模板官方接口：

```javascript
function getvar(key, options = {});
function getLocalVar(key, options = {});
function getGlobalVar(key, options = {});
function getMessageVar(key, options = {});
```

最常用写法：

```javascript
getvar('stat_data.时间系统.日期')
getvar('stat_data.位置系统.地点名称')
getvar('stat_data.核心状态._魅力')
```

这里的 `key` 不是随便的字符串，而是基于 `lodash _.get` 的“路径”。

也就是说：

- `stat_data.时间系统.日期` 表示读对象 `stat_data -> 时间系统 -> 日期`
- `stat_data.关系系统.在场人物` 表示读数组或对象路径
- 如果你先拿整个对象，也可以再用正常 JS 访问

例如：

```ejs
<%_
const stat = getvar('stat_data') || {};
const date = stat['时间系统']?.['日期'];
_%>
<%- date %>
```

#### `getvar('stat_data.时间系统.日期')` 实际是从哪读的

这点非常关键。

提示词模板内部会先构造一个 `variables` 缓存对象，然后 `getvar(...)` 默认读这个缓存。当前源码里的合并顺序是：

1. 全局变量 `global`
2. 初始变量 `initial`
3. 聊天变量 `local/chat`
4. 消息变量 `message`

后面的会覆盖前面的。

所以：

> `getvar('stat_data.时间系统.日期')` 默认并不是“死读某个固定文件”，而是读“当前轮上下文里合并后的最新值”。

在 `MoST-2-7-7` 里，`stat_data` 通常最终来自“最新消息楼层变量”，因为 MVU 会把每轮更新后的变量回写到消息变量里。

#### 常用 `options`

最常用的是这几个：

- `scope`
  - `global`
  - `local`
  - `message`
  - `cache`
  - `initial`
- `defaults`
  - 变量不存在时返回默认值
- `noCache`
  - 刚写完变量又要立刻读时才需要
- `clone`
  - 返回深拷贝，避免直接改引用

例子：

```javascript
getvar('stat_data.核心状态._幸运', { defaults: 0 })
getvar('stat_data', { clone: true })
getvar('某变量', { scope: 'global' })
```

#### 这张卡里的真实例子

`EJS大型事件控制器`：

```ejs
const evtDateStr = getvar('stat_data.时间系统.日期') || '';
```

作用：

- 读当前日期
- 决定是否导入 `sj偶像总条目`、`sj运动会总条目`、`sj学院祭总条目`

`EJS地图控制器`：

```ejs
const currentLocName = getvar('stat_data.位置系统.地点名称') || '';
```

作用：

- 读当前地点
- 决定导入哪个真实地点条目

`EJS魅力阶段控制器`：

```ejs
const charmValue = Number(getvar('stat_data.核心状态._魅力')) || 0;
```

作用：

- 读魅力数值
- 输出对应阶段说明文本

---

### 18.3 `setvar / incvar / decvar / delvar / insvar`：写变量

提示词模板里同族接口很多，最常用的是：

```javascript
function setvar(key, value, options = {});
function incvar(key, value = 1, options = {});
function decvar(key, value = 1, options = {});
function delvar(key, options = {});
function insvar(key, value, index = undefined, options = {});
```

这一家更适合：

- 在模板执行阶段自己维护状态
- 给别的控制器预先写缓存值
- 做小规模计数

但要注意：

> `MoST-2-7-7` 当前主流变量闭环不是靠 `setvar(...)`，而是靠 MVU 解析 `<UpdateVariable>` 后统一回写。

也就是说，在这张卡里：

- EJS 控制器主要负责“读变量 + 导入条目”
- 真正的状态推进主要还是交给 MVU

所以你在这张卡里看到的 `getvar(...)` 远多于 `setvar(...)`，这是设计使然。

---

### 18.4 `getwi(...)` 一家：读世界书条目

提示词模板官方接口：

```javascript
async function getwi(lorebook, title, data = {});
async function getWorldInfo(lorebook, title, data = {});
async function getwi(title, data = {});
async function getWorldInfo(title, data = {});
```

这家接口是当前卡最核心的动态拼装工具。

#### 作用

直接读取某个世界书条目的内容，并把它插回当前模板执行结果。

#### 和原生世界书的区别

- 原生世界书：靠关键词扫描后激活
- `getwi(...)`：你主动点名读取，绕过原生扫描逻辑

#### `lorebook` 参数怎么写

当前源码里，如果你不传世界书名，或者传 `null` / 空值，它会自动回退到当前角色主世界书。

所以当前卡大量写：

```ejs
<%- await getwi(null, activatedMainEvent) %>
```

这里的 `null` 实际含义是：

> “就在当前角色绑定的主世界书 `MoST_College` 里找条目。”

#### `title` 参数到底支持什么

当前源码里，匹配逻辑是：

1. `data.comment === title`
2. `data.uid === title`
3. `data.comment.match(title)`

这意味着 `title` 可以是：

- 条目标题
- 条目 uid
- 正则表达式
- 看起来像正则的字符串

所以这张卡里这句是成立的：

```ejs
<%- await getwi(null, `^${charName}$`) %>
```

它的真实意图是：

> 用正则精确匹配标题，避免“夏洛特”顺手把“夏洛特好感事件”一起匹配出来。

#### 这张卡里的真实例子

`EJS大型事件控制器`：

```ejs
if (activatedMainEvent) {
_%>
<%- await getwi(null, activatedMainEvent) %>
<%_
}
```

作用：

- 先算出当前该进哪个大型事件阶段
- 再把对应条目正文拉进 prompt

`EJS角色控制器`：

```ejs
<%_ for (const charName of charactersToLoad) { _%>
    <%- await getwi(null, `^${charName}$`) %>
<%_ } _%>
```

作用：

- 遍历“当前在场人物 + 最近用户输入提到的人物”
- 把这些人物对应的角色条目逐个导入

`EJS地图控制器`：

```ejs
<%- await getwi(null, matchedLocation.target) %>
```

作用：

- 把“第七教学楼”“钟楼”这种玩家视角地点名，映射成真正的地点条目标题后再导入

---

### 18.5 `activewi(...)`：激活而不是直接读取

提示词模板官方接口：

```javascript
async function activewi(lorebook, title, force = false);
async function activateWorldInfo(lorebook, title, force = false);
async function activewi(title, force = false);
```

和 `getwi(...)` 的区别只有一句话：

- `getwi(...)`：直接把条目内容拿出来
- `activewi(...)`：把条目标记为“激活”，再交给酒馆原生世界书流程处理

当前 `MoST-2-7-7` 主要用的是 `getwi(...)`，不是 `activewi(...)`。

原因很现实：

> 作者要的是“我指定谁进 prompt”，而不是“我把它放回原生扫描器里等一轮”。

---

### 18.6 `matchChatMessages(...)`：扫最近消息

提示词模板官方接口：

```javascript
function matchChatMessages(pattern, options = {});
```

常用参数：

- `start`
- `end`
- `role`
- `and`

当前卡真实例子：

```ejs
if (matchChatMessages(characterName, { start: -1 })) {
    mentionedCharacters.push(characterName);
}
```

作用：

- 只扫最后一条消息
- 如果用户刚刚输入里提到了某个角色名，就把对应角色条目也导入

这里 `start: -1` 可以简单理解为：

> “只看最后一条消息。”

所以 `EJS角色控制器` 并不只依赖 `在场人物`，它还会看用户刚刚主动提到了谁。

---

### 18.7 其他你会用到的提示词模板接口

这一组在当前卡里不是主力，但写自己的卡时很常用：

| 接口 | 用途 |
| --- | --- |
| `execute('/setvar ...')` | 直接执行酒馆 slash 命令 |
| `getchar(name, template, data)` | 读取角色卡定义 |
| `getpreset(name, data)` | 读取预设提示词内容 |
| `getqr(name, label, data)` | 读取快速回复内容 |
| `define(name, value, merge)` | 定义全局函数或变量给后续模板复用 |
| `print(...args)` | 在 `<% ... %>` 内主动输出文本 |

#### 常用上下文变量

提示词模板在执行时，还会提供一些现成上下文：

| 名称 | 含义 |
| --- | --- |
| `variables` | 当前缓存变量合集 |
| `SillyTavern` | `SillyTavern.getContext()` 返回值 |
| `faker` | 随机数据工具 |
| `model` | 当前生成所用模型名 |
| `lastUserMessage` | 最近一条用户消息 |
| `lastCharMessage` | 最近一条角色消息 |
| `chatId` | 当前聊天 ID |
| `characterId` | 当前角色 ID |

---

## 十九、酒馆助手接口速查

这一层不是在 prompt 里跑的，而是在“角色脚本 / 前端脚本”里跑的。

### 19.1 变量宏：`{{format_message_variable::...}}`

当前卡最关键的一个酒馆助手宏就是：

```text
{{format_message_variable::stat_data}}
```

它出现在条目 `变量列表` 里：

```text
<status_current_variable>
{{format_message_variable::stat_data}}
</status_current_variable>
```

作用：

- 读取最新消息楼层变量中的 `stat_data`
- 用 YAML 形式格式化
- 放进 prompt 让模型读

#### `get_` 和 `format_` 的区别

| 写法 | 输出形式 |
| --- | --- |
| `{{get_message_variable::stat_data}}` | 一行 JSON 字符串 |
| `{{format_message_variable::stat_data}}` | 多行 YAML 块 |

对模型来说：

> `format_` 版通常更清晰，所以在变量框架卡里一般优先用它。

#### 一个很重要的细节：它会自动过滤 `$` 开头字段

酒馆助手宏的官方说明和本地源码都表明：

> `{{get/format_xxx_variable::...}}` 会忽略所有以 `$` 开头的键。

所以：

- `$属性点`
- `$基础魅力`
- `$基础幸运`

这类字段即使存在于 `stat_data` 里，也不会通过 `{{format_message_variable::stat_data}}` 发给模型。

但：

- `_魅力`
- `_幸运`
- `_段位`

不会被这个宏自动隐藏。

这也解释了为什么这张卡大量使用：

- `$前缀` 存内部字段
- `_前缀` 存只读但可让模型感知的字段

再结合当前卡自己的规则：

```yaml
- don't update field names starts with `_` as they are readonly
```

你可以把它理解成：

- `$字段`：默认不发给模型
- `_字段`：会发给模型，但作者约定它们只读

这是这张卡非常关键的约定。

---

### 19.2 酒馆助手变量类型，和提示词模板的 `scope` 不是一回事

这点很容易混。

#### 提示词模板 `getvar/setvar` 的作用域

- `global`
- `local`
- `message`
- `cache`
- `initial`

#### 酒馆助手 `getVariables(...)` 的类型

- `global`
- `chat`
- `character`
- `preset`
- `message`
- `script`

所以不要把：

- 提示词模板的 `local`
- 酒馆助手的 `chat`

当成两个不同概念，它们在很多场景下其实都在说“当前聊天绑定的数据”，只是接口体系不同。

---

### 19.3 `getVariables(...)`：脚本里直接取变量

酒馆助手官方接口：

```javascript
function getVariables(option: VariableOption): Record<string, any>
```

例子：

```javascript
const variables = getVariables({ type: 'message', message_id: -1 });
```

作用：

- 在脚本里直接拿最新消息楼层变量
- 常用于状态栏脚本

老版 MVU 教程和很多状态栏示例里会写：

```javascript
const data = window.TavernHelper.getVariables({ type: 'message', message_id: message_id });
```

然后再读：

```javascript
data.stat_data
data.display_data
```

---

### 19.4 事件系统：`eventOn / eventEmit / eventOnce`

酒馆助手官方接口：

```javascript
function eventOn(event_type, listener)
function eventOnce(event_type, listener)
function eventEmit(event_type, ...data)
```

最常见用法：

```javascript
eventOn(tavern_events.MESSAGE_RECEIVED, () => {
  console.log('收到一条回复');
});
```

在 `MoST` 生态里，这一层最常见的用途是：

- 监听按钮点击
- 监听酒馆消息事件
- 监听 MVU 事件

---

### 19.5 脚本按钮：`getButtonEvent(...)`

酒馆助手官方接口：

```javascript
function getButtonEvent(button_name: string): string;
```

最常见写法：

```javascript
const eventType = getButtonEvent('打开状态栏');
eventOn(eventType, () => {
  console.log('按钮被点了');
});
```

这张卡里的真实按钮包括：

- MVU 脚本
  - `重新处理变量`
  - `重新读取初始变量`
- 性斗学园脚本
  - `打开状态栏`

也就是说，角色卡 JSON 里那堆脚本按钮，不只是 UI 装饰，它们最终都能被映射成事件。

---

### 19.6 共享接口：`initializeGlobal / waitGlobalInitialized`

酒馆助手官方接口：

```javascript
function initializeGlobal(global: string, value: any): void;
function waitGlobalInitialized<T>(global: string): Promise<T>;
```

典型场景：

```javascript
await waitGlobalInitialized('Mvu');
```

官方文档也明确建议：

> 在使用 `Mvu` 前，应该先等待它初始化完毕。

这张卡的外置 `性斗学园脚本` 就属于这一类脚本：

- 先等 MVU 就绪
- 再读 `Mvu.getMvuData(...)`
- 再监听 `Mvu.events.VARIABLE_UPDATE_ENDED`

---

## 二十、MVU 接口速查

### 20.1 当前卡用的是新版 MVU 思路

这一点必须先说清楚。

你能在网上看到两种 MVU 写法：

1. 旧教程常见写法
   - `_.set('角色.好感度', 33, 35);`
   - 很多变量是 `[值, 说明]`
2. 当前 `MoST-2-7-7` 实际写法
   - `<UpdateVariable><JSONPatch>...</JSONPatch></UpdateVariable>`
   - `stat_data` 主体以普通对象字段为主

所以：

> 如果旧教程和这张卡冲突，请优先以这张卡当前世界书和当前 MVU beta 源码为准。

---

### 20.2 Mvu 全局对象

当前源码导出的核心接口是：

```javascript
Mvu.getMvuData(options)
Mvu.replaceMvuData(mvu_data, options)
Mvu.parseMessage(message, old_data)
Mvu.isDuringExtraAnalysis()
Mvu.events
```

其中最常用的是：

```javascript
const data = Mvu.getMvuData({ type: 'message', message_id: 'latest' });
await Mvu.replaceMvuData(data, { type: 'message', message_id: 'latest' });
```

#### `Mvu.events`

当前可用事件里最重要的几个是：

- `VARIABLE_INITIALIZED`
- `VARIABLE_UPDATE_STARTED`
- `COMMAND_PARSED`
- `VARIABLE_UPDATE_ENDED`
- `BEFORE_MESSAGE_UPDATE`

当前卡最常用的是：

```javascript
eventOn(Mvu.events.VARIABLE_UPDATE_ENDED, (variables, variables_before_update) => {
  // 重算派生属性、刷新状态栏
});
```

---

### 20.3 `stat_data / display_data / delta_data`

从当前 MVU beta 源码看，`MvuData` 里主要有这些字段：

- `stat_data`
  - 实际状态值
- `schema`
  - 当前变量结构约束
- `display_data`
  - 给 UI 展示用的变化文本
- `delta_data`
  - 本轮变化摘要

但源码也已经把 `display_data` 和 `delta_data` 标成了 `@deprecated`。

所以对当前卡来说，最稳妥的理解是：

- 规则控制器主要读 `stat_data`
- 状态栏脚本有时会顺手读 `display_data`

---

### 20.4 `[initvar]` 和 `<initvar>`：初始变量到底怎么进来

这是你刚才追问的核心点之一。

#### 世界书初始化

当前 MVU beta 源码里，`loadInitVarData(...)` 会扫描知识书条目名，只要：

```text
entry.comment?.toLowerCase().includes('[initvar]')
```

就会把这个条目当成初始化条目。

而且它会：

1. 读条目内容
2. 如果包了 `<initvar>...</initvar>`，就只取里面的内容
3. 如果包了代码块 ```...```，就剥掉代码块外壳
4. 再按 YAML/JSON 解析
5. 合并进 `mvu_data.stat_data`

所以，这张卡里的 `[initvar]` 条目不是摆设，它就是初始化种子之一。

#### 开局消息覆盖

当前 MVU beta 源码还支持另一层：

```text
<initvar> ... </initvar>
```

如果这个块写在开局问候语里，MVU 会把它当成“开局覆盖初始化值”。

源码里的注释写得很直接：

> 使用了 `<initvar>` 块后，会以 `<initvar>` 块内的内容为基准，忽略角色世界书中 `[initvar]` 的内容。

这意味着：

- `[initvar]` 负责通用默认值
- `<initvar>` 负责某个开局分支的覆盖值

---

### 20.5 `JSONPatch` 路径到底怎么写

这是新版 MVU 最容易写错的点。

当前卡要求模型输出：

```xml
<UpdateVariable>
<Analysis>...</Analysis>
<JSONPatch>
[
  { "op": "replace", "path": "/时间系统/日期", "value": "2025年03月18日" }
]
</JSONPatch>
</UpdateVariable>
```

注意：

> 这里的 `path` 是相对于 `stat_data` 根的，不要再写 `/stat_data/时间系统/日期`。

这是当前 MVU beta 源码明确证明的：

1. 它先把 JSON Patch 的 `/a/b/c` 转成点路径 `a.b.c`
2. 然后直接作用到 `variables.stat_data`

所以正确理解是：

| 你所在层 | 正确路径 |
| --- | --- |
| `getvar(...)` 读变量 | `stat_data.时间系统.日期` |
| `JSONPatch` 写变量 | `/时间系统/日期` |
| 脚本里拿整包 `MvuData` | `data.stat_data.时间系统.日期` |

#### 四种操作

当前卡自己的 `[mvu_update]变量输出格式` 已经约定了 4 种：

- `replace`
- `delta`
- `insert`
- `remove`

例子：

```json
[
  { "op": "replace", "path": "/时间系统/日期", "value": "2025年03月18日" },
  { "op": "delta", "path": "/核心状态/堕落度", "value": 2 },
  { "op": "insert", "path": "/关系系统/新角色", "value": { "好感度": 10, "关系类型": "陌生人" } },
  { "op": "remove", "path": "/任务系统/支线任务/旧任务" }
]
```

---

## 二十一、`stat_data.时间系统.日期` 是怎么来的

现在可以把这条链讲完整了。

### 第 1 层：初始值种子

这张卡里有一个 `[initvar]` 条目，内容中明确写了：

```yaml
时间系统:
  日期: "2025年03月17日"
  星期: 1
  时间: "08:00"
```

这就是它的默认开局日期。

### 第 2 层：MVU 初始化装载

MVU 初始化时会扫描条目名里包含 `[initvar]` 的世界书条目，并把它解析进：

```text
mvu_data.stat_data
```

所以此时变量树里会出现：

```text
stat_data.时间系统.日期
```

### 第 3 层：提示词模板读取缓存

提示词模板 `getvar(...)` 默认读的是合并后的缓存变量。

在当前卡里，最终通常会解析到：

```text
最新消息楼层变量 -> stat_data -> 时间系统 -> 日期
```

所以控制器里写：

```javascript
getvar('stat_data.时间系统.日期')
```

就能拿到当前日期。

### 第 4 层：模型看到当前值

条目 `变量列表` 会把：

```text
{{format_message_variable::stat_data}}
```

展开成 YAML。

因此模型每次生成前，都能在 prompt 里看到当前 `stat_data`，包括当前日期。

### 第 5 层：模型输出增量更新

模型在回复末尾输出：

```xml
<UpdateVariable>
<JSONPatch>
[
  { "op": "replace", "path": "/时间系统/日期", "value": "2025年03月18日" }
]
</JSONPatch>
</UpdateVariable>
```

### 第 6 层：MVU 回写

MVU 把这个 patch 应用到 `variables.stat_data`。

于是下一轮 `getvar('stat_data.时间系统.日期')` 读到的就是新日期。

#### 一句话总结

> `stat_data.时间系统.日期` 的来源链是：`[initvar] 默认值 -> MVU 初始化 -> message 变量快照 -> getvar(...) 读取 -> JSONPatch 更新 -> 下一轮继续读取`。

---

### 21.1 一个非常容易混淆的补充：它不是 Prompt Template 标准的 `[InitialVariables]`

提示词模板源码里自己的“初始变量”标准入口只认：

- `[InitialVariables]`
- `@@initial_variables`

而这张卡写的是：

- `[initvar]`

所以：

> 这张卡的 `[initvar]` 不是走 `ST-Prompt-Template` 标准初始变量加载器，而是走 MVU 自己的 initvar 机制。

这个区别一定要记住。

否则你会误以为：

- `[initvar]` 是提示词模板原生关键字

其实不是。

---

## 二十二、同一个变量的三套路径写法

还是拿“日期”举例。

| 场景 | 写法 | 谁来解释 |
| --- | --- | --- |
| EJS 里读变量 | `getvar('stat_data.时间系统.日期')` | 提示词模板，按 lodash 点路径解释 |
| 酒馆助手宏输出 | `{{format_message_variable::stat_data.时间系统.日期}}` | 酒馆助手宏，按路径解释 |
| MVU JSONPatch 写变量 | `"/时间系统/日期"` | MVU，把它当 `stat_data` 根下的 JSON Pointer |
| 脚本里直接读对象 | `data.stat_data.时间系统.日期` | 普通 JavaScript 对象访问 |

所以千万别写成：

```json
{ "op": "replace", "path": "/stat_data/时间系统/日期", "value": "..." }
```

对当前 MVU 来说，这通常是多套了一层。

---

## 二十三、用 `MoST-2-7-7` 直接举 5 个可抄的模板

### 23.1 时间驱动事件条目

```ejs
<%_
const date = getvar('stat_data.时间系统.日期') || '';
let entry = '';
if (date.includes('6月9日') || date.includes('06-09')) {
  entry = '多校联赛';
}
_%>
<% if (entry) { %>
<%- await getwi(null, entry) %>
<% } %>
```

适合：

- 节日
- 大型事件
- 每月固定比赛

---

### 23.2 地点映射导入

```ejs
<%_
const loc = getvar('stat_data.位置系统.地点名称') || '';
const map = [
  { target: '中心广场', triggers: ['中心广场', '中央广场'] },
  { target: '博览图书馆', triggers: ['图书馆', '博览图书馆'] },
];
const matched = map.find(x => x.triggers.some(t => loc.includes(t)));
_%>
<% if (matched) { %>
<%- await getwi(null, matched.target) %>
<% } %>
```

适合：

- 伪装地点名映射回真实条目名
- 多个别名汇总到一个条目

---

### 23.3 最近输入提名触发角色条目

```ejs
<%_
const names = ['夏洛特', '柳烟霞', '莉莉丝'];
const hit = names.filter(name => matchChatMessages(name, { start: -1 }));
_%>
<%_ for (const name of hit) { _%>
<%- await getwi(null, `^${name}$`) %>
<%_ } _%>
```

适合：

- 用户主动点名某角色时，强制补载该角色条目

---

### 23.4 数值转阶段文本

```ejs
<%_
const value = Number(getvar('stat_data.核心状态._幸运')) || 0;
_%>
<% if (value >= 100) { %>
高幸运阶段规则...
<% } else if (value >= 50) { %>
中幸运阶段规则...
<% } else { %>
低幸运阶段规则...
<% } %>
```

适合：

- 魅力
- 幸运
- 堕落度
- 好感阶段

---

### 23.5 当前卡最常见的“完整闭环模板”

1. 用 `getvar('stat_data....')` 读状态。
2. 用普通 JS 判断当前分支。
3. 用 `getwi(null, 标题)` 拉条目。
4. 用 `{{format_message_variable::stat_data}}` 把当前变量发给模型。
5. 模型输出 `<UpdateVariable><JSONPatch>...</JSONPatch></UpdateVariable>`。
6. MVU 回写 `stat_data`。
7. 外置脚本监听 `Mvu.events.VARIABLE_UPDATE_ENDED` 做二次计算。

这就是 `MoST-2-7-7` 的实际工作方式。

---

## 二十四、最后给你一个“看见语法就知道它在干嘛”的速查表

| 看到的写法 | 你应该立刻想到什么 |
| --- | --- |
| `getvar('stat_data.时间系统.日期')` | 在 EJS 控制器里读当前状态变量 |
| `getwi(null, '条目名')` | 直接把当前世界书某个条目拉进 prompt |
| `getwi(null, '^夏洛特$')` | 用正则精确匹配标题，避免串到别的条目 |
| `activewi(...)` | 不是直接读，而是重新交给原生世界书激活 |
| `matchChatMessages(name, { start: -1 })` | 检查用户最后一句有没有提到某角色 |
| `{{format_message_variable::stat_data}}` | 把最新消息楼层变量中的 `stat_data` 以 YAML 形式发给模型 |
| `$字段` | 会被酒馆助手变量宏隐藏，不直接发给模型 |
| `_字段` | 当前卡约定为只读字段，模型能看到但不该更新 |
| `[initvar]` | MVU 初始化条目，不是 Prompt Template 原生 `[InitialVariables]` |
| `<initvar>...</initvar>` | 开局消息内覆盖默认初始化值 |
| `<UpdateVariable><JSONPatch>...</JSONPatch>` | 这轮回复末尾要交给 MVU 执行的状态更新 |
| `"/时间系统/日期"` | MVU 的 JSONPatch 路径，默认相对 `stat_data` 根 |

---

## 二十五、如果你以后要自己写卡，最实用的分工建议

1. 把“状态值”统一放在 `stat_data`。
2. 把“控制器”写成 EJS 条目，只做判断和导入，不要在里面塞太多静态设定正文。
3. 把“可复用设定模块”拆成普通世界书条目，让控制器用 `getwi(...)` 拉。
4. 把“初始化默认值”写进 `[initvar]`，把“特定开局覆盖值”写进开场白 `<initvar>...</initvar>`。
5. 把“UI 和派生计算”放到酒馆助手脚本里，用 `Mvu.events.VARIABLE_UPDATE_ENDED` 监听，不要把这部分硬塞进 prompt。

做到这 5 点，你的卡就会比单纯堆词条稳定得多。
