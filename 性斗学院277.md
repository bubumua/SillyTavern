# 性斗学院 2-7-7（MoST）动态世界书编排分析

## 结论
这张 `MoST-2-7-7.json` 不是“只靠酒馆原生关键词扫描”在工作，而是一个混合编排系统。它至少同时用了 5 层机制：

1. SillyTavern 原生世界书注入。
2. `ST-Prompt-Template` 提供的 EJS 模板执行与 `getwi/getvar` 递归导入。
3. `MVU` 提供的变量更新、变量解析、剧情 AI / 变量 AI 分流能力。
4. `性斗学园脚本` 对 MVU 更新后的变量做二次计算、状态栏同步和名字规范化。
5. regex + 远程 UI 片段，把 `<content>` / `<parallel>` / `<option>` / `<UpdateVariable>` 这些结构化输出变成前端展示。

所以它的核心逻辑不是“哪些词条被扫到”，而是“先放哪些静态规则，再由控制器按变量递归拉起哪些隐藏/禁用条目，最后再由 MVU 和前端脚本把回复拆成正文、选项、变量更新和 UI 展示”。

## 我实际核对到的对象
- 目标卡：`preset_and_chara/chara/MoST/MoST-2-7-7.json`
- 本卡总词条数：217
- 启用词条数：37
- 正则脚本数：22
- 启用正则脚本数：10
- 启用 `tavern_helper` 脚本：5 个
- 额外本地调试脚本：1 个，已禁用

我同时核对了这些外部或本地逻辑来源：
- `default-user/extensions/ST-Prompt-Template/docs/reference_cn.md`
- `default-user/extensions/ST-Prompt-Template/src/function/worldinfo.ts`
- `tmp_mvu_bundle.js`
- `tmp_mvu_zod.js`
- `tmp_wi_sort.js`
- `tmp_auto_update_char.js`
- `tmp_xdxy_main.js`
- `public/scripts/world-info.js`
- `public/scripts/openai.js`

## 这张卡实际用了哪几种“激活方式”
不是所有条目都走同一套触发逻辑。当前卡内至少有 4 种激活来源并存。

### 1. 原生常量条目
这类条目 `constant: true`，会直接进入当前 prompt，不靠关键词。

典型例子：
- `核心世界观`
- `战斗系统`
- `地图`
- `主线`
- `人物列表`
- `势力列表`
- `[mvu_plot]并行事件规则`
- `[mvu_update]变量更新规则`
- `正文格式（随AI输出）`

### 2. 原生 selective 条目

这类条目仍然由酒馆原生扫描决定是否激活。

当前启用的 selective 条目只有 3 个：
- `开局COT`
  - 关键字：`【学员档案】`
- `性斗演义生成规则（可自行更改或关闭）`
  - 关键字：`[战斗日志]`
- `欲望竞技场_人物`
  - 关键字：`[4-4]`、`[4-5]`、`竞技场`

这说明这张卡仍保留了一部分“传统世界书扫描”的用法，但它已经不是主导逻辑。

### 3. EJS 控制器条目
这是这张卡最关键的一层。

当前启用的 EJS 条目有：
- `EJS大型事件控制器`
- `EJS角色控制器`
- `EJS魅力阶段控制器`
- `EJS幸运阶段控制器`
- `EJS堕落度阶段控制器`
- `EJS地图控制器`
- `EJS比赛控制器`
- `外校列表`

这些条目会在注入 prompt 时执行模板代码，通过：
- `getvar(...)` 读取当前变量
- `getwi(...)` 按标题递归导入别的世界书条目

### 4. MVU 标签分层
这张卡还把一部分条目用命名约定做了分层：
- `[mvu_plot]...`
- `[mvu_update]...`
- 无标签公共条目

这在默认“随AI输出”模式下主要是语义分层，在切到 MVU 的“额外模型解析”模式时，就会变成真正的提示词分流规则。

## 外部脚本各自负责什么

| 脚本 | 作用 | 是否直接参与动态世界书编排 |
| --- | --- | --- |
| `世界书强制自定义排序` | 把 `#world_info_sort_order` 锁到自定义排序，并禁用切换 | 间接参与 |
| `MVU` | 解析变量更新、暴露 `window.parent.Mvu`、支持额外模型解析 | 直接参与 |
| `Zod 变量结构` | 给 `stat_data` 注册 schema，校验/修正变量结构 | 直接参与变量侧 |
| `自动更新角色卡` | 拉远程卡和更新日志，提供更新按钮 | 不参与运行时拼装 |
| `性斗学园脚本` | 监听 MVU 更新，做二次计算、状态栏 UI、名字规范化 | 不负责选条，但负责更新后的再处理 |

### 1. 世界书强制自定义排序
脚本内容非常简单，本质是：
- 把 `#world_info_sort_order` 设置成 `13`
- 触发 change
- 禁止用户改回其他排序方式

它不是主逻辑，但很重要，因为作者显然依赖“自定义排序”维护词条顺序，避免编辑器切换到其他排序方式后把人类维护顺序搞乱。

### 2. MVU
这是运行时的第二核心。

我在 `tmp_mvu_bundle.js` 里确认到几件关键事：
- MVU 会把对象挂到 `window.parent.Mvu`
- 挂载后会发出 `global_Mvu_initialized`
- MVU 有自己的事件常量，其中包含 `VARIABLE_UPDATE_ENDED`
- MVU 的“额外模型解析”帮助文本明确写着：
  - 变量更新 AI 只会使用 `[mvu_update]` 条目
  - 变量更新 AI 还会使用“既没有 `[mvu_plot]` 也没有 `[mvu_update]` 的条目”
  - 变量更新 AI 只看“最后两楼消息”
  - 如果勾选“发送预设”，还会附带 preset 提示词
- 对“未适配额外模型解析”的世界书，会被当作 `[mvu_plot]`，也就是：
  - 只发给剧情 AI
  - 不发给变量更新 AI

这就解释了为什么这张卡要把一部分条目命名成 `[mvu_plot]` / `[mvu_update]`。

### 3. Zod 变量结构
这段内联脚本从远程导入 `registerMvuSchema`，再给 `stat_data` 注册一整套很大的 schema。

它的作用不是选词条，而是：
- 限制变量结构必须符合预期
- 拦截不合法 patch
- 在变量初始化和变量更新后做合法化/纠错

所以它属于“变量侧护栏”。

### 4. 自动更新角色卡
这个脚本只负责：
- 读取远程角色卡链接
- 读取远程更新日志
- 给 UI 挂“更新角色卡”“更新日志”按钮

它不参与 prompt 拼装，不参与世界书动态激活。

### 5. 性斗学园脚本
这是运行时第三核心，但它不负责“选哪些条目进 prompt”，它负责“变量更新后怎么再算一遍状态”。

我在 `tmp_xdxy_main.js` 里确认到：
- 脚本会等待 MVU 初始化
- 会读取 `Mvu.getMvuData({ type: 'message', message_id: 'latest' })`
- 会调用 `Mvu.replaceMvuData(...)`
- 会监听 `Mvu.events.VARIABLE_UPDATE_ENDED`
- 会在这个事件后：
  - 规范化 `关系系统` 名称
  - 规范化 `在场人物`
  - 自动修正异常值
  - 更新段位
  - 重算衍生属性
  - 维护状态栏展示
- 还会给角色脚本按钮注册 `打开状态栏`

所以这层可以理解为：
- MVU 负责“把变量改掉”
- 性斗学园脚本负责“变量改完以后，把依赖它的派生状态重新算出来”

## 真正的动态编排主流程
如果按一次正常生成来拆，顺序大致是这样：

1. SillyTavern 先做常规世界书扫描和 prompt 组装。
2. 原生 constant / selective 条目先后进入 `worldInfoBefore` 和 `worldInfoAfter`。
3. 这些条目中，带 EJS 的条目在 `ST-Prompt-Template` 下会被执行。
4. EJS 控制器读取 `stat_data`，再按条件递归 `getwi(...)` 拉起别的条目。
5. 主模型收到“静态条目 + EJS 动态展开后的条目 + 角色卡正文”。
6. 主模型按 `正文格式（随AI输出）` 产出：
   - `<content>`
   - `<parallel>`
   - `<option>`
   - `<UpdateVariable>`
7. MVU 解析 `<UpdateVariable>` 中的 `<JSONPatch>`，更新 `stat_data`。
8. Zod schema 校验 patch 结果。
9. `性斗学园脚本` 监听 `VARIABLE_UPDATE_ENDED`，重算派生属性、段位、状态栏数据。
10. regex 脚本把结构化标签从“后续 prompt 里”隐藏，同时把回复替换成远程 UI 片段做前端展示。

可以把它压缩成一句话：

> 先让世界书控制器按变量把世界观拼出来，再让模型按固定格式输出，再让 MVU 把变量吃回去，最后让前端脚本把这些结构化结果变成可视化界面。

## 从“系统边界”再看一遍：到底谁负责什么
上面讲的是流程，这里换一个角度，按“责任边界”拆。

### 1. SillyTavern 核心负责什么
SillyTavern 核心负责的是“大框架”：
- 读取角色卡和世界书数据
- 按 `before_char` / `after_char` 分区
- 执行原生 constant / selective 激活
- 把结果作为 `worldInfoBefore` / `worldInfoAfter`
- 最终按 `worldInfoBefore -> main -> worldInfoAfter` 放进聊天补全 prompt

它并不理解这张卡里的：
- `getwi(...)`
- `getvar(...)`
- `<% ... %>`
- `[mvu_plot]`
- `[mvu_update]`

这些都不是 SillyTavern 原生世界书扫描规则本身。

### 2. ST-Prompt-Template 负责什么
`ST-Prompt-Template` 负责的是“把普通世界书条目变成可执行模板模块”。

它补上的能力是：
- EJS 执行
- 变量读取 `getvar(...)`
- 递归拉条目 `getwi(...)`
- 递归时自动推断当前世界书名
- 读取禁用词条数据后按标题精确导入

所以如果没有它，这张卡最关键的几组控制器基本都会失效：
- `EJS大型事件控制器`
- `EJS角色控制器`
- `EJS地图控制器`
- `EJS比赛控制器`

### 3. 主剧情模型负责什么
主剧情模型负责的是“消费拼好的世界观并产出结构化回复”。

在当前导出配置下，它拿到的是：
- 常驻规则
- EJS 动态展开后的事件、地点、人物、状态规则
- 当前角色卡主体
- 当前 `stat_data` 变量文本
- 回复格式约束

它被要求在同一条回复里同时完成：
- 写正文
- 写并行事件
- 写选项
- 写变量更新

### 4. MVU 负责什么
MVU 负责的是“理解 `<UpdateVariable>` 并把它落回变量系统”。

它不是选剧情条目的那一层，而是状态机落地层：
- 解析 `<Analysis>`
- 解析 `<JSONPatch>`
- 应用 patch
- 发事件
- 在额外模型解析模式下，把剧情写作和变量更新拆成两路请求

### 5. 性斗学园脚本负责什么
它负责“把更新后的原始变量再变成可玩的游戏状态”。

具体包括：
- 自动修正异常值
- 重算等级 / 段位
- 规范化名字
- 维护状态栏、技能、地图等 UI 所依赖的数据

所以真正的职责边界是：

> SillyTavern 核心搭骨架，`ST-Prompt-Template` 做动态拼装，主模型负责写结果，MVU 负责吃回变量，`性斗学园脚本` 负责把变量再加工成系统状态。

## 这张卡的 prompt 拼装顺序
这里要分两层理解：
- 大层顺序：`before_char -> 主角色卡正文 -> after_char`
- 小层顺序：每个区块内部再按当前导出 JSON 的 `insertion_order` 排

`openai.js` 里我确认到主链路是：

- `worldInfoBefore`
- `main`
- `worldInfoAfter`

也就是这张卡的 `before_char` 会在角色卡主体之前，`after_char` 会在角色卡主体之后。

### before_char 区域
以下是当前启用条目中最重要的 `before_char` 顺序：

| 顺序 | 条目 | 作用 |
| --- | --- | --- |
| -2 | `COT （简短版）该COT是让你缝入预设的，不是直接开的` | 给剧情思考框架 |
| -1 | `开局COT` | 看到 `【学员档案】` 时追加开局思考 |
| 0 | `===世界观开始===` | 分段标记 |
| 10 | `核心世界观` | 基础世界规则 |
| 11 | `战斗系统` | 段位、比赛规则 |
| 13 | `地图` | 全图总览 |
| 14-17 | `文风条目` / `No_Gay` / 可疑空条目 / `性斗演义生成规则` | 风格、限制和战斗日志扩展 |
| 30 | `数值` | 基础数值相关说明 |
| 50 | `===世界观结束===` / `EJS大型事件控制器` | 结束基础世界观，并按日期拉大事件 |
| 100 | `主线` | 主线、日程、大型事件总览 |
| 290 | `人物列表` | 核心人物总表 |
| 291 | `外校列表` | 日期满足时动态显示外校名单 |

### 角色卡主体
中间是角色卡自己的主体内容，也就是主角色定义。

### after_char 区域
以下是当前启用条目中最重要的 `after_char` 顺序：

| 顺序 | 条目 | 作用 |
| --- | --- | --- |
| 0 | `user` | 主角学员档案 |
| 20 | `势力列表` | 学园势力关系 |
| 80-82 | `EJS比赛控制器` / `EJS魅力阶段控制器` / `EJS幸运阶段控制器` / `EJS堕落度阶段控制器` | 比赛事件和状态阶段说明 |
| 200 | `===地点开始===` | 地点段起点 |
| 201 | `EJS地图控制器` | 按当前位置导入具体地点条目 |
| 249 | `欲望竞技场_人物` | 原生 selective 地点人物条目 |
| 261 | `===地点结束===` | 地点段终点 |
| 310 | `===人物开始===` | 人物段起点 |
| 311 | `EJS角色控制器` | 按在场人物和用户输入拉角色条目 |
| 390 | `===人物结束===` | 人物段终点 |
| 8000 | `[mvu_plot]并行事件规则` | 并行事件的剧情侧规范 |
| 8001-8002 | `[mvu_update]经验值规则` / `[mvu_update]势力声望规则` | 变量更新规则补充 |
| 9000 | `变量列表` | 把当前 `stat_data` 输出进 prompt |
| 9100 | `正文格式（随AI输出）` | 限定输出标签结构 |
| 9997-9998 | `[mvu_update]变量更新规则` / `[mvu_update]变量输出格式` | 变量更新规则与输出格式 |

## 一个更接近实际的 prompt 剖面
如果把这张卡当前一次生成前的 prompt 用“抽象剖面”的方式写出来，大致会像这样：

```text
[worldInfoBefore]
  COT （简短版）
  开局COT（如果命中）
  核心世界观
  战斗系统
  地图
  文风条目
  No_Gay
  性斗演义生成规则（如果命中）
  数值
  EJS大型事件控制器 -> 动态导入 sj偶像/运动会/学院祭条目
  主线
  人物列表
  外校列表（日期满足时才输出）

[main]
  角色卡主体

[worldInfoAfter]
  user
  势力列表
  EJS比赛控制器 -> 动态导入 多校联赛/校内赛/擂台赛/黑暗决赛
  EJS魅力阶段控制器 -> 输出当前魅力阶段说明
  EJS幸运阶段控制器 -> 输出当前幸运阶段说明
  EJS堕落度阶段控制器 -> 输出当前堕落阶段说明
  EJS地图控制器 -> 动态导入当前地点条目
  欲望竞技场_人物（如果原生关键字命中）
  EJS角色控制器 -> 动态导入当前人物与隐藏事件条目
  [mvu_plot]并行事件规则
  [mvu_update]经验值规则
  [mvu_update]势力声望规则
  变量列表 -> format_message_variable::stat_data
  正文格式（随AI输出）
  [mvu_update]变量更新规则
  [mvu_update]变量输出格式
```

这个剖面最重要的意义在于：
- 不是所有信息都常驻
- 大量“场景 / 人物 / 事件细节”都是控制器运行后才临时展开的
- `变量列表` 和格式约束是放在很靠后的区域，用来把当前状态和输出协议压给模型

也就是说，模型看到的不是一份固定大设定，而是一份“按当前变量裁切过的设定快照”。

## 各个控制器到底怎么动态拉条目

### 1. EJS大型事件控制器
这是“按时间推进主事件”的控制器。

它会：
- 读取 `stat_data.时间系统.日期`
- 以 `2025-03-17` 为起点算 `passedDays`
- 根据经过天数决定激活哪个大型事件
- 再通过 `getwi(null, ...)` 拉对应世界书条目

当前逻辑是：
- 第 9 到 13 天：拉 `sj偶像总条目` + `sj偶像阶段0~4`
- 第 24 到 27 天：拉 `sj运动会总条目` + `sj运动会阶段0~3`
- 第 37 到 44 天：拉 `sj学院祭总条目`
  - 如果是前 5 天，还会再拉 `sj学院祭阶段0~4`

也就是说，大型事件不是靠关键字碰运气，而是明确由当前日期驱动。

### 2. 外校列表
`外校列表` 本身也是 EJS。

它会：
- 读取 `stat_data.时间系统.日期`
- 同样从 `2025-03-17` 计算经过天数
- 当 `foreignPassedDays >= 37` 时，才把外校角色总表输出到 prompt

这相当于一个“时间门”：
- 第 37 天前，外校资料整体不出现
- 第 37 天后，外校总览开始出现

### 3. EJS比赛控制器
这是“按日期和地点给当前比赛补规则”的控制器。

它会读：
- `stat_data.时间系统.星期`
- `stat_data.时间系统.日期`
- `stat_data.位置系统.地点名称`

然后按优先级激活：
- `多校联赛`
- `校内赛`
- `擂台赛`
- `黑暗决赛`

导入方式是：
- `await getwi(null, activatedEvent)`

这意味着比赛规则不是常驻整包注入，而是只在当前日期/场景对应的比赛被拉进来。

### 4. EJS地图控制器
这是“按当前位置拉地点条目”的控制器。

它会读：
- `stat_data.位置系统.地点名称`

然后用一个地点映射表，把：
- 伪装名称
- 真实名称

统一映射到真正的目标条目，再执行：
- `await getwi(null, matchedLocation.target)`

这使得地点世界书从“整张地图总表”变成了：
- `地图` 提供全局索引
- `EJS地图控制器` 只按当前地点拉当前场景需要的具体地点说明

### 5. EJS角色控制器
这是最复杂、也最像“动态编排器”的控制器。

它会同时参考 4 类信息：
- `stat_data.关系系统.在场人物`
- `stat_data.角色基础._姓名`
- 当前日期对应的时间门
- 用户最后一条输入里提到的人物名

它的内部逻辑是：

1. 先把 `在场人物` 取出来。
2. 再把主角姓名也加入候选列表。
3. 计算 `passedDays`。
4. 建立两套匹配表：
   - 长名字：用关键词部分匹配
   - 短名字：要求完全匹配
5. 再定义一批“时间锁角色”：
   - `timeLockedNames`
   - `timeLockedSpecialEntries`
6. 再用 `matchChatMessages(characterName, { start: -1 })` 检查用户最后一条输入中是否直接提到了某个角色。
7. 把要加载的角色名放进 `charactersToLoad`。
8. 最后循环执行：
   - `await getwi(null, ${charName})`

这里有几个特别重要的点。

#### 角色控制器不是只看“在场人物”
它还会扫描用户最后一条输入。

这意味着如果玩家主动点名某角色，即使该角色还不在 `在场人物` 中，也可能被提前拉入 prompt。

#### 它用正则锚定标题精确取条目
导入时使用的是：
- `getwi(null, \`^${charName}$\`)`

这表示它不是模糊搜内容，而是精确匹配“条目标题/备注名”。

#### 它把“隐藏事件条目”也纳入角色导入链
控制器里有：
- `timeLockedSpecialEntries = ["夏洛特好感事件"]`
- 如果满足条件，还会把 `夏洛特好感事件` 也塞进 `charactersToLoad`

而 `夏洛特好感事件` 这个条目本身在 JSON 里是：
- `enabled: false`
- `selective: true`
- `keys: []`

也就是说，它根本不是给酒馆原生扫描直接激活的，而是故意做成“平时禁用，只能由 EJS 控制器手动拉起”的隐藏事件条目。

这点非常关键。

我在 `ST-Prompt-Template` 的文档和源码里确认到：
- `getwi/getWorldInfo` 的递归导入是扩展自己负责的
- `getWorldInfoEntry(...)` 会从 `getWorldInfoEntries()` 里找条目
- 文档明确写了 `getWorldInfoEntries()` “Even disabled entries are returned”

所以结论是：

> 这张卡确实在利用 `getwi` 越过酒馆原生“启用/禁用扫描”的常规入口，把禁用条目当作私有子模块按需导入。

这已经不是传统世界书写法，而是“世界书条目模块化”。

### 6. EJS魅力 / 幸运 / 堕落度阶段控制器
这三类控制器不负责再拉其他世界书条目，但它们会动态生成说明文本。

它们各自读取：
- `stat_data.核心状态._魅力`
- `stat_data.核心状态._幸运`
- `stat_data.核心状态.堕落度`

然后按区间输出当前阶段的规则说明和效果描述。

它们的作用是把“数值状态”实时翻译成 prompt 里的软规则。

## 这张卡的默认变量更新模式
从启用条目来看，这张卡当前默认是偏向 **“随AI输出”** 模式的。

证据有 3 个：
- 启用的是 `正文格式（随AI输出）`
- 启用的是 `[mvu_update]变量输出格式`
- 该格式要求主模型在同一条回复尾部直接输出 `<UpdateVariable>`

同时卡里还存在两条相关但未启用的条目：
- `[mvu_plot]正文格式（额外模型解析）`
- `[mvu_update]变量输出格式强调`

这说明作者显然考虑过 MVU 的“额外模型解析”路线，但当前导出的启用状态里，主工作流仍然是：

> 同一个剧情模型先写正文，再在末尾直接输出变量更新。

如果用户手动在 MVU 面板里切到“额外模型解析”，那时 `[mvu_plot]` / `[mvu_update]` 的分层才会彻底变成双路提示词系统。

## “随AI输出” 与 “额外模型解析” 的真实区别
这部分值得单独展开，因为它直接影响你理解“哪些条目发给谁”。

| 模式 | 剧情正文由谁写 | 变量更新由谁写 | `[mvu_plot]` / `[mvu_update]` 的意义 |
| --- | --- | --- | --- |
| 随AI输出 | 主模型 | 同一个主模型 | 主要是提示词分层和约束命名 |
| 额外模型解析 | 主模型 | 额外变量模型 | 真的变成两套路由规则 |

### 在“随AI输出”里
当前卡的启用状态就是这个模式。

此时：
- 主模型一次性收到大部分 prompt
- 主模型既负责剧情又负责变量更新
- `[mvu_update]` 条目仍然有用，因为它会告诉主模型如何写 `<UpdateVariable>`
- `[mvu_plot]` 条目仍然有用，因为它会告诉主模型如何写 `<parallel>`

但本质上，这两类条目还没有被真正拆给两个不同的模型。

### 在“额外模型解析”里
如果切到这个模式，逻辑就变成：

1. 主模型先正常产出剧情回复。
2. MVU 再单独组织一份“变量更新专用 prompt”。
3. 这份专用 prompt 只发给变量模型。

根据 MVU 源码里的帮助说明，这份“变量更新专用 prompt”会优先包含：
- 名字里有 `[mvu_update]` 的世界书条目
- 名字里既没有 `[mvu_plot]` 也没有 `[mvu_update]` 的公共条目
- 仅最后两楼消息
- 可选的 preset 提示词

而名字里是 `[mvu_plot]` 的条目，会偏向剧情侧，不喂给变量模型。

所以：

> `随AI输出` 是“一个模型兼任编剧和记账员”；`额外模型解析` 是“一个模型写戏，另一个模型单独记账”。

这也解释了为什么卡里会同时存在：
- `正文格式（随AI输出）`
- `[mvu_plot]正文格式（额外模型解析）`

它们其实是在为两种不同工作流预留协议。

## regex 和远程 UI 资源怎么配合
启用的 regex 可以分成两组。

### 1. 对 AI 隐藏类
这组脚本都带 `promptOnly: true`，作用是避免结构化输出在后续轮次再次污染 prompt。

它们会从后续 prompt 中移除：
- `<UpdateVariable> ... </UpdateVariable>`
- `<option> ... </option>`
- `【【】】`
- `<StatusPlaceHolderImpl/>`

所以这层是“清理回灌内容”。

### 2. UI 渲染类
这组脚本会把回复中的结构标签替换成远程 UI 片段加载逻辑。

对应关系是：
- `正文UI`
- `并行事件UI`
- `选项UI`
- `变量更新UI`
- `战斗UI`
- `开局美化`

这些脚本本身不参与世界书动态选择，但它们定义了：
- 世界书和模型必须按什么标签输出
- 输出后前端会怎样显示

所以它们属于“显示协议”的一部分。

## 一个简化版的真实调用例子
如果当前变量是这样：

- 日期：`2025-04-24`
- 星期：`4`
- 地点名称：`中央广场`
- 在场人物：`["夏洛特"]`
- 用户最后一句额外提到了 `柳烟霞`

那么这张卡的动态逻辑大致会变成：

1. `EJS大型事件控制器`
   - 判定进入 `sj学院祭`
   - 导入 `sj学院祭总条目`
   - 视阶段再导入某个 `sj学院祭阶段X`
2. `外校列表`
   - 因为经过天数 >= 37，输出外校总览
3. `EJS地图控制器`
   - 把 `中央广场` 映射到真实地点条目
   - 导入对应地点条目
4. `EJS角色控制器`
   - 因为 `在场人物` 有 `夏洛特`，导入 `^夏洛特$`
   - 因为玩家最后一句提到 `柳烟霞`，再导入 `^柳烟霞$`
   - 若时间锁条件满足，还可能额外导入 `夏洛特好感事件`
5. 主模型收到完整拼装结果
6. 按 `正文格式（随AI输出）` 输出正文、并行事件、选项、变量更新
7. MVU 解析更新
8. 性斗学园脚本重算派生状态
9. regex 替换显示

## 把这个例子再展开成“一整轮完整时序”
上面的例子还是偏摘要，这里改成更接近真实调用顺序的版本。

假设当前状态仍然是：
- 日期：`2025-04-24`
- 星期：`4`
- 地点名称：`中央广场`
- 在场人物：`["夏洛特"]`
- 玩家最后一句提到：`柳烟霞`

那么一次完整轮次可以拆成下面 14 步。

### 第 1 步：酒馆先收集原生世界书
SillyTavern 先从当前角色卡世界书里取出：
- 启用 constant 条目
- 命中的 selective 条目
- 并按 `before_char` / `after_char` 分区

这一步还是酒馆原生逻辑。

### 第 2 步：`before_char` 先进入候选 prompt
这时已经有一部分固定内容进入 `worldInfoBefore`，比如：
- `核心世界观`
- `战斗系统`
- `地图`
- `主线`
- `人物列表`

但里面带 EJS 的条目还没“静态化展开完”。

### 第 3 步：`ST-Prompt-Template` 执行 `EJS大型事件控制器`
它读取日期，算出：
- 当前已进入 `sj学院祭`

于是递归导入：
- `sj学院祭总条目`
- 对应阶段条目

这些内容不是预先常驻在 prompt 里，而是控制器运行后临时拼进去。

### 第 4 步：`ST-Prompt-Template` 执行 `外校列表`
因为 `passedDays >= 37`，于是外校总览开始出现在 prompt 中。

注意，这一步只是“外校总表开始出现”，不等于所有外校角色个人条目都已经出现。

### 第 5 步：角色卡主体进入 `main`
这时主角色定义、角色卡正文、角色设定等主体内容进入 `main`。

### 第 6 步：`after_char` 固定条目进入候选 prompt
例如：
- `user`
- `势力列表`
- `变量列表`
- `正文格式（随AI输出）`
- `[mvu_update]变量更新规则`

### 第 7 步：执行 `EJS比赛控制器`
它读取：
- 日期
- 星期
- 地点

然后判断当前是否该导入某种比赛规则条目。

如果命中，就把对应比赛条目塞进 `after_char` 的有效 prompt。

### 第 8 步：执行 `EJS地图控制器`
它读取：
- `stat_data.位置系统.地点名称 = 中央广场`

然后通过地点映射表，把这个地点映射到真正的地点条目名，并递归导入对应地点条目。

这一步的结果是：
- 模型不会看到整张地图的全部地点细节
- 只会看到当前地点对应的那一块局部场景说明

### 第 9 步：执行 `EJS角色控制器`
这是这一轮最关键的一步。

控制器会综合：
- 在场人物里的 `夏洛特`
- 玩家输入里提到的 `柳烟霞`
- 主角自己的姓名
- 时间门限制

于是可能导入：
- `^夏洛特$`
- `^柳烟霞$`
- 如果条件满足，再导入 `夏洛特好感事件`

这一步结束后，“当前和谁互动、有哪些人被显式搬进上下文”才真正定型。

### 第 10 步：状态阶段条目把数值翻译成软规则
接着魅力 / 幸运 / 堕落度控制器会把当前数值翻译成自然语言规则。

例如：
- 当前魅力处于哪个阶段
- 当前幸运处于哪个阶段
- 当前堕落度处于哪个阶段

这些描述不会改变量，但会强烈影响剧情模型的表现。

### 第 11 步：主模型看到最终拼装后的 prompt
到这一步，主模型真正收到的上下文已经不是原始 JSON，而是：
- 当前日期对应的大事件
- 当前地点对应的局部地点设定
- 当前人物对应的角色条目
- 当前状态对应的数值阶段说明
- 当前变量快照
- 当前输出格式协议

所以这一步的上下文是高度裁切过、情境化过的。

### 第 12 步：主模型输出结构化回复
在当前卡的默认模式下，主模型需要一次性输出：
- `<content>`
- `<parallel>`
- `<option>`
- `<UpdateVariable>`

这里它既是“剧情编剧”，又是“变量更新员”。

### 第 13 步：MVU 解析变量更新
MVU 会取出：
- `<Analysis>`
- `<JSONPatch>`

然后把 patch 应用到 `stat_data`。

如果注册了 Zod schema，就在这一步附近做变量结构校验和必要修正。

### 第 14 步：性斗学园脚本做二次加工
当 MVU 发出 `VARIABLE_UPDATE_ENDED` 后：
- `性斗学园脚本` 会读取最新 `stat_data`
- 做名字规范化
- 做段位 / 属性 / 派生数值重算
- 刷新状态栏所依赖的数据

于是下一轮 EJS 控制器再读 `getvar(...)` 时，拿到的就已经是“更新后且再加工后”的变量状态。

这一步非常重要，因为它把整个系统闭环了：

> 上一轮剧情输出的变量更新，会在这一轮变成新的事件条件、地点条件、人物条件和数值条件。

## 为什么这种结构会比纯静态世界书更强
把这个机制理解透以后，就能看出作者为什么不用“单纯堆很多条目”的写法。

### 1. 上下文体积更可控

如果把所有人物、地点、事件全常驻塞进 prompt：
- token 会爆炸
- 相关性会下降
- 模型容易漂移

而控制器只按当前变量拉当前所需条目，所以上下文更瘦。

### 2. 事件可以像代码一样被调度
像：
- `sj学院祭总条目`
- `sj学院祭阶段X`
- `夏洛特好感事件`

这些都已经不是“死条目”，而是像函数模块一样被调用。

### 3. 变量和设定形成闭环
这张卡不是：
- 先写设定
- 再让模型自由发挥

而是：
- 变量决定当前加载哪些设定
- 模型根据这些设定写剧情
- 剧情再更新变量
- 变量再决定下一轮加载哪些设定

这已经非常接近“脚本化 RPG 状态机”。

## 我认为最值得注意的两个细节

### 1. `夏洛特好感事件` 是“禁用但可递归导入”的私有条目
这不是异常，而是作者刻意把某些剧情做成“只允许控制器手动调度”的模块。

这是整张卡最典型的动态编排手法之一。

### 2. 外校相关时间门存在一个明显不一致
我看到 3 个时间门：
- `EJS大型事件控制器` 中，`sj学院祭` 从 `passedDays >= 37` 开始
- `外校列表` 中，外校总览从 `foreignPassedDays >= 37` 开始
- `EJS角色控制器` 中，外校角色加载门槛却写成了 `festivalStartDay = 65`

这意味着当前导出版本下，可能会出现这种情况：
- 外校总览已经进入 prompt
- 学院祭主事件也已经进入 prompt
- 但 `EJS角色控制器` 仍然不会把那批“时间锁外校角色”条目拉进来

如果这不是作者故意延后角色级条目触发，那它很可能就是一个时间门写错了的逻辑风险点。

## 最终判断
`MoST-2-7-7` 的核心不是“大型世界书”，而是“世界书条目模块化 + EJS 控制器调度 + MVU 变量回写 + 前端 UI 协议”。

更准确地说，它把角色卡/世界书拆成了 4 类模块：
- 常驻规则模块
- 条件触发模块
- 控制器模块
- 后处理与展示模块

其中真正负责“动态编排世界书内容”的，不是 MVU，也不是 regex，而是：
- `ST-Prompt-Template` 提供的 `getvar/getwi`
- 以及卡内这几个 EJS 控制器

MVU 负责的是“把这套剧情状态机真正跑起来”，`性斗学园脚本` 负责的是“把跑出来的变量进一步变成可玩的系统和 UI”。
